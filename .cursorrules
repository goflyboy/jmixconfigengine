
## 核心规则：日志内容必须使用英文

### 🚫 禁止在日志中使用非英文字符
- 所有 `log.info()`, `log.warn()`, `log.error()`, `log.debug()` 消息必须使用纯英文
- 禁止使用中文、日文、韩文等非英文字符
- 日志消息应该简洁、清晰、专业

### ✅ 正确的日志示例
```java
log.info("User authentication successful for user: {}", username);
log.warn("Database connection timeout, retrying...");
log.error("Failed to process request: {}", errorMessage);
log.debug("Processing item: {} with value: {}", itemId, itemValue);
```

### ❌ 错误的日志示例
```java
log.info("用户认证成功，用户名: {}", username);
log.warn("数据库连接超时，正在重试...");
log.error("处理请求失败: {}", errorMessage);
log.debug("正在处理项目: {}，值: {}", itemId, itemValue);
```

### 📝 注释可以使用中文
- JavaDoc注释可以使用中文，便于中文开发者理解
- 行内注释可以使用中文，解释复杂的业务逻辑
- 已有中文注释必须保持中文，不得强行翻译成英文
- 注解/JSON中的中文自然语言字段（如 `normalNaturalCode`、description）必须保持中文
- 编辑包含中文的源码、文档和资源文件时必须使用 UTF-8
- 提交前必须扫描乱码特征：替换字符 `U+FFFD`、私用区字符，以及常见 mojibake 码点（如 `U+9205`, `U+9227`, `U+95BA`, `U+940E`, `U+9346`, `U+95B0`, `U+6D60`, `U+93C4`, `U+7459`, `U+59AF`, `U+7029`, `U+95AE`, `U+9359`, `U+9352`, `U+93C1`, `U+9422`, `U+93AC`）。
- 只有日志内容必须使用英文

### 🔍 日志检查规则
- 使用正则表达式 `[\u4e00-\u9fff]` 检查日志中的中文字符
- 在代码审查时重点关注日志内容
- 确保所有日志消息符合国际化标准

### 💡 最佳实践
1. 使用标准的英文技术术语
2. 保持消息的简洁性和可读性
3. 使用适当的标点符号
4. 避免使用缩写（除非是标准缩写）
5. 在异常日志中记录足够的上下文信息

---
# JMix Config Engine - Cursor Rules
# 语言/风格（Language & Style）
- 所有输出均使用简体中文，包括思考过程、分析、计划与最终回答。
- 仅当用户明确要求其它语言时才切换语言。
- 代码、API 名称、日志字符串、类名/函数名、URL 保持原文，不做翻译。

# 表达（Clarity）
- 优先用要点式中文说明，尽量简洁；必要时使用 Markdown 代码块展示代码。
- 避免过度冗长；先给结论，再给必要细节。

# 与项目规则协同（Repo-specific）
- 虽然回复用中文，但代码中的日志字符串必须是英文（与项目规则一致）。
- 注释可以使用中文。
- 以前是中文的注释、JavaDoc、注解说明和JSON自然语言字段必须继续保持中文，不要翻译成英文。
- 所有包含中文的文件按 UTF-8 读写；提交前检查乱码字符和 mojibake 片段。

# 交互（Interaction）
- 若用户未指定，默认继续使用简体中文进行后续对话与思考过程输出。


## 核心规则：日志内容必须使用英文

### 🚫 禁止在日志中使用非英文字符
- 所有 `log.info()`, `log.warn()`, `log.error()`, `log.debug()` 消息必须使用纯英文
- 禁止使用中文、日文、韩文等非英文字符
- 日志消息应该简洁、清晰、专业

### ✅ 正确的日志示例
```java
log.info("User authentication successful for user: {}", username);
log.warn("Database connection timeout, retrying...");
log.error("Failed to process request: {}", errorMessage);
log.debug("Processing item: {} with value: {}", itemId, itemValue);
```

### ❌ 错误的日志示例
```java
log.info("用户认证成功，用户名: {}", username);
log.warn("数据库连接超时，正在重试...");
log.error("处理请求失败: {}", errorMessage);
log.debug("正在处理项目: {}，值: {}", itemId, itemValue);
```

### 📝 注释可以使用中文
- JavaDoc注释可以使用中文，便于中文开发者理解
- 行内注释可以使用中文，解释复杂的业务逻辑
- 只有日志内容必须使用英文

### 🔍 日志检查规则
- 使用正则表达式 `[\u4e00-\u9fff]` 检查日志中的中文字符
- 在代码审查时重点关注日志内容
- 确保所有日志消息符合国际化标准

### 💡 最佳实践
1. 使用标准的英文技术术语
2. 保持消息的简洁性和可读性
3. 使用适当的标点符号
4. 避免使用缩写（除非是标准缩写）
5. 在异常日志中记录足够的上下文信息

---

# Git 提交规范（Git Commit）

## 默认提交行为
- **默认提交到 `main` 分支**，不需要创建额外的功能分支。
- 只有用户明确要求创建分支时才创建分支。

## 需要提交的文件类型
- `.specstory/` 目录下的文件（会话历史、统计等）需要提交。
- 其他非标准文件类型（如 IDE 配置、AI 会话记录等）**默认都需要提交**，除非用户明确说明忽略。

---

**重要提醒：日志是给运维人员和开发者看的，应该使用国际通用的英文；注释是给开发者看的，可以使用中文便于理解。**
