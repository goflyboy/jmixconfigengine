package com.jmix.ruletrans.scenario;

/**
 * Coarse rule family used to select prompts, converters, and scenario tests.
 */
public enum RuleFamily {
    COMPATIBLE,
    CALCULATE,
    AGGREGATE,
    SELECT,
    HIDDEN,
    PRIORITY,
    STRUCTURED,
    POST,
    UNKNOWN
}
