模块级自然语言规则：
${naturalLanguage}

模块 code：
${moduleCode}

可用的 PartCategory 列表：
${availableCategories}

请识别这条规则涉及的 PartCategory code。
要求：
- 只返回 JSON 字符串数组，不要返回解释、Markdown 或其他文本。
- 数组元素必须来自上面的 PartCategory code，保持原始大小写和英文缩写，不要翻译、不要改写。
- 可以根据中文名称、英文名称、属性 code、属性值或自然语言同义表达来判断分类，但返回值只能是 code。

返回示例：
["cpu","drive"]

如果无法识别任何分类，返回 []。
