自然语言规则：
${naturalLanguage}

上下文：
${contextSummary}

上下文 JSON：
${contextJson}

已生成的规则方法体：
${methodBody}

请生成用于验证该规则的结构化测试用例。
只返回简洁 JSON，并匹配以下 schema：
{
  "ruleMethod": "optional method name",
  "cases": [
    {
      "id": "shortCaseId",
      "type": "validate",
      "selectedParts": ["partCode1", "partCode2"],
      "expectedValid": false,
      "expectedViolatedRuleCodes": ["ruleCode"]
    },
    {
      "id": "shortRecommendCaseId",
      "type": "recommend",
      "requests": ["category:Sum_Quantity ==1 where Attr=Value"],
      "expectedResult": "NO_SOLUTION"
    }
  ]
}

selected part 组合校验使用 validate case；推理/求解路径使用 recommend case。
普通规则校验优先生成 validate case。
在 validate case 中，selectedParts 是一组互不重复的已选原子部件 code。
不要重复某个 part code 来表达数量。
只有当自然语言规则明确要求推荐、推理、求解或无解行为时，才生成 recommend case。
对 recommend case，expectedResult 只能是 "NO_SOLUTION" 或 "HAS_SOLUTION"。
不要在 expectedResult 中放入具体部件 code、解列表或自由文本。
