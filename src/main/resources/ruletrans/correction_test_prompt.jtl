生成的规则方法体已通过编译，但测试失败。

自然语言规则：
${naturalLanguage}

上下文：
${contextSummary}

上下文 JSON：
${contextJson}

SDK profile：
${sdkProfile}

允许使用的 SDK API：
${allowedSdkApis}

禁止使用的 SDK API：
${forbiddenSdkApis}

上一版方法体：
${previousMethodBody}

失败测试用例：
${failedCases}

如果失败很可能是规则逻辑错误，只返回修正后的 Java 规则方法体。
不要包含 package、import、class 声明、注解、方法声明、Markdown 或解释说明。
只使用能编译通过并匹配 SDK profile 的 ModuleAlgBase API。

当 SDK profile 为 POST 时，只使用 POST 实例视图 API，例如 parameter(code).setValue(...)、
part(code).setQuantity(...)、partCategory(code).parameter(code).setValue(...)、
partCategory(code).part(code).setQuantity(...)、partCategory(code).sumQuantity()
和 partCategorySum(code).sumDynAttr4Int(...)。数值写回参数时使用 String.valueOf(...) 包装。
不要使用 model()、onlyEnforceIf(...)、inCompatible(...) 或 priority API。
POST 分类数量总和使用 partCategory("categoryCode").sumQuantity()。
partCategorySum("categoryCode") 只用于动态属性聚合，例如
sumDynAttr4Int(...)、dynAttr(...) 或 dynAttrs(...)。绝不要调用
partCategorySum("categoryCode").sumQuantity() 或 partCategorySum("categoryCode").quantity()。

当 SDK profile 为 CONSTRAINT 时，使用约束模型 API，例如 model().sum4Selected(...)、
model().sum4Quantity(...)、model().addLessOrEqual(...)、model().addGreaterOrEqual(...)、
model().addEquality(...) 和 inCompatible(ruleCode, "category:Attr=value", "category:Attr=value")。
model().addBoolAnd 和 model().addBoolOr 只接受一个 AlgCPLiteral[] 参数。绑定 BoolVar 到
conjunction 或 disjunction 时，需要用 onlyEnforceIf 同时绑定 true 和 false 两个方向；
不要调用 model().addBoolAnd(cond, a, b) 或 model().addBoolOr(cond, a, b)。
model().addEquality(...)、model().addDifferent(...)、model().addLessOrEqual(...)、
model().addGreaterOrEqual(...) 以及类似数值 API 返回 AlgCPConstraint，不是 AlgCPLiteral。
不要把这些调用放入 AlgCPLiteral[]，也不要传给 model().addImplication(...)。
如需把数值谓词作为布尔 literal 使用，先创建 AlgCPBoolVar，并用
addEquality(...).onlyEnforceIf(eqVar) 和 addDifferent(...).onlyEnforceIf(eqVar.not()) 双向绑定。
model().addImplication(left, right) 只接受 AlgCPLiteral 参数，例如 BoolVar、hiddenVar()、
option(...).selectedVar() 或它们的 .not() 形式。
线性表达式项使用 addTerm(variable, coefficient)，例如
expr.addTerm(para("p1").valueVar(), 1)。不要反转参数顺序。
组合 model().sum4Quantity(...) 或 model().sum4Selected(...) 这类聚合 PartAlgCPLinearExpr 时，
使用 compositeExpr.addExpr(aggregateExpr, coefficient)。不要把聚合表达式传给 addTerm(...)。
PartAlgCPLinearExpr 也是线性表达式。它可用于数值约束，例如
model().addGreaterOrEqual(totalExpr, para("p1").valueVar())。当聚合表达式与参数比较时，
使用 para("p1").valueVar()，不要直接使用 ParaVar。
隐藏辅助方法使用 addVarAboutHiddenConstraints(para("p1"), para("p2")) 或
addVarAboutHiddenConstraints(partVar("part1"), partVar("part2"))。不要传入 ruleCode、hiddenVar()、
valueVar() 或 BoolVar，也不要通过 model() 调用。
如果规则可能隐藏某个参数或部件，必须对每个 hiddenVar() 被规则控制的参数或部件调用
addVarAboutHiddenConstraints(...)。否则引擎会添加默认可见约束，隐藏解可能变得不可行。
对聚合数量或按动态属性加权的总量，优先使用
model().sum4Quantity("categoryCodeOrCommaSeparatedCodes", "attrCodeOrBlank", "optional Attr=value filter");
attrCode 参数表示数值型加权属性，不是过滤属性。对 selected-count 规则，
attrCode 使用 ""，属性谓词放在 filter condition 中。对部件总数量规则也使用 attrCode ""，
除非规则明确要求按数值动态属性加权。
跨分类模块级总量使用逗号分隔分类 code；模块级多实例全部实例总量使用 categoryCode*；
part-category effectScope=AllInst 规则应使用双参数 overload：
model().sum4Quantity("attrCode", "filter") 或 model().sum4Selected("attrCode", "filter")。
不要因为分类支持多实例就自动添加 *；只有自然语言明确要求该分类的全部实例时才使用 *。
不要在一次聚合调用中混用 * 和逗号分隔分类，例如 "disk*,mainBoard"；如果真实规则需要这种形态，
应分别构建聚合表达式，再用 model().newPartLinearExpr(...).addExpr(...) 组合。
聚合总量不应通过枚举原子部件并使用 partVar(...).quantityVar() 构建。
在 filter condition 中使用上下文 dynAttr 中的动态属性值，或 option 的 codeValue，不要使用 option code。
filter condition 是规则引擎字符串，例如 Attr=value 或 Attr>=number；不要使用 Java ==，
也不要给字符串值额外加引号。
对优先级规则，先添加普通硬约束。然后使用 model().newPartLinearExpr("name") 创建
PartAlgCPLinearExpr 作为单独的目标表达式，用 addExpr(...) 组合聚合表达式，
调用 model().setObjectExpr(expr)，再调用 updatePriorityObjectFuntion(ruleCode, expr)。
model().newPartLinearExpr 需要 name 字符串。
不要使用 POST 实例视图 API，例如 parameter(...).setValue(...)、partCategorySum(...)、
part(...) 或 partCategory(...)。
