The generated rule method body passed compilation but failed tests.

Natural-language rule:
${naturalLanguage}

Context:
${contextSummary}

Context JSON:
${contextJson}

SDK profile:
${sdkProfile}

Allowed SDK APIs:
${allowedSdkApis}

Forbidden SDK APIs:
${forbiddenSdkApis}

Previous method body:
${previousMethodBody}

Failed test cases:
${failedCases}

Return only a corrected Java rule method body if the failure is likely a rule logic error.
Do not include package, imports, class declarations, annotations, method declarations,
markdown, or explanations.
Use only ModuleAlgBase APIs that compile, such as model().sum4Selected(...),
model().sum4Quantity(...), model().addLessOrEqual(...), model().addGreaterOrEqual(...),
model().addEqual(...), and inCompatible(ruleCode, "category:Attr=value", "category:Attr=value").
