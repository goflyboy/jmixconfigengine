Natural-language rule:
${naturalLanguage}

Context:
${contextSummary}

Target category codes:
${targetCategories}

PartCategory JSON:
${contextJson}

SDK profile:
${sdkProfile}

Allowed SDK APIs:
${allowedSdkApis}

Forbidden SDK APIs:
${forbiddenSdkApis}

Generate exactly one Java rule method body for a ModuleAlgBase subclass rule method.
Requirements:
- Return only Java statements for the method body.
- Do not include package, imports, class declarations, annotations, method declarations,
  markdown, or explanations.
- @CodeRuleAnno, method name, method header, and rule identifier are generated by RuleTrans templates.
- If an API requires a rule code, use the existing local variable named ruleCode.
- For category selection counts, use model().sum4Selected("categoryCode", "", "optional Attr=value filter").
- For category quantity sums, use model().sum4Quantity("categoryCode", "", "optional Attr=value filter").
- For numeric constraints, use model().addLessOrEqual(...), model().addGreaterOrEqual(...),
  or model().addEqual(...).
- For incompatibility, call inCompatible("ruleCode", "category:Attr=value", "category:Attr=value")
  with string expressions only.
- Do not call partCategoryVar(...).count(), totalQuantityVariable(), selectedCount(),
  model().addConstraint(...), or inCompatible(partVar(...), partVar(...)).
- Do not use POST instance-view APIs such as currentInst(), parameter(...).setValue(...),
  partCategorySum(...), part(...), partCategory(...), or setDynAttr(...).
- Prefer business-friendly expressions and category codes from the context.

Valid examples:
model().addLessOrEqual(model().sum4Selected("cpu", "", ""), 1);
