当前需要按穷举预设法实现输入多实例，请帮我完成输出详细设计，请按面向对象的方法进行设计

一、什么要输入多实例？
  在配置中（或运行态），一个部件分类有多组输入要求，且每组的输入要求是独立的 
  
基础数据如下：（spec1代表属性的定义，如容量，spec2V2代表属性值，如10G，Drive-B代表部件分类，drive1代表部件,Q代表配置数量，S代表配置的是否选中,logicA代表规则
--Server  logicAB
-----Drive-A	 容量,spec2,spec3,...  logicA1,logicA2
-------drive1 10T spec2V1,spec3V1,...,Q,S
-------drive2 20T spec2V2,spec3V2,...,Q,S
-------drive3 30T spec2V3,spec3V3,...,Q,S
-----CPU-B	 内存,spec2,spec3,... logicB1
-------cpu1 256G spec2V1,spec3V1,...,Q,S
-------cpu2 512G spec2V2,spec3V2,...,Q,S
-------cpu2 128G spec2V3,spec3V3,...,Q,S


标书要求配置"配置一台服务器，CPU是4核，要求第一组容量20T，类型为机械硬盘， 第二组容量30T，容量固态硬盘（两种输入要求是独立的）

最后的配置结果（输出数据）
  --ServerConfig 
  ----Drive-I0
  -------drive1Config Q=2 
  ----Drive-I1
  -------drive3Config Q=1 
  ----CPU-I0
  -------cpu2Config Q=1 

二、穷举预设法的方案思路
 
要点1、类似在开发基础数据时，按配置的需求先预设几个实例，如Drive-A预设两个用例 
维护态：仅维护部件分类，并指定其实例code
--Server  logicAB(变化），logicA2(变化）
-----Drive-A	 容量,spec2,spec3,...  logicA1,   instCods="DriveI0,DriveI1"
-------drive1 10T spec2V1,spec3V1,...,Q,S
-------drive2 20T spec2V2,spec3V2,...,Q,S
-------drive3 30T spec2V3,spec3V3,...,Q,S
 
-----CPU-B	 内存,spec2,spec3,... logicB1
-------cpu1 256G spec2V1,spec3V1,...,Q,S
-------cpu2 512G spec2V2,spec3V2,...,Q,S
-------cpu2 128G spec2V3,spec3V3,...,Q,S

发布态是把Drive按实例Id展开
--Server  logicAB(变化），logicA2(变化）
-----DriveI0-A	 容量,spec2,spec3,...  logicA1,
-------drive1 10T spec2V1,spec3V1,...,Q,S
-------drive2 20T spec2V2,spec3V2,...,Q,S
-------drive3 30T spec2V3,spec3V3,...,Q,S

-----DriveI1-A	 容量,spec2,spec3,...  logicA1, 
-------drive1 10T spec2V1,spec3V1,...,Q,S
-------drive2 20T spec2V2,spec3V2,...,Q,S
-------drive3 30T spec2V3,spec3V3,...,Q,S

-----CPU-B	 内存,spec2,spec3,... logicB1
-------cpu1 256G spec2V1,spec3V1,...,Q,S
-------cpu2 512G spec2V2,spec3V2,...,Q,S
-------cpu2 128G spec2V3,spec3V3,...,Q,S

按照当前
DriveI0和DirveI1的drive1的实例名称会重复，需要增加PartCategory的实例,来区分？


详细的变化请见：MultiReqTest的实例（没有预设前请参考BaseOptiTest）


要点2：输出的要求

    @Test
    public void oneReq() {
        // Natural-Input: 要求机械硬盘容量>=5T, 要求4核的CPU的总内存>=512G
        inferRecommendModule("driveI0:sum.Capacity >=5 where Speed=5400", "cpu:sum.Memory >=512 where CoreNum=4");
        printSimpleSolutions();
        // 变化点5-1：要求为保持输入的简洁见，如果仅输出一个实例，则和原来多单实例一样，不加实例名
        assertSoluContain(1, "cpu2(Q:2,H:0,S:1),md1(Q:5,H:0,S:1),sd1(0*)");
        assertSoluContain("cpu2(Q:20,H:0,S:1),md1(Q:5,H:0,S:1),sd1(0*)");
    }

    @Test
    public void twoReq() {
        // Natural-Input: 要求机械硬盘容量>=5T, 要求机械硬盘容量>=5T, 要求4核的CPU的总内存>=512G
        inferRecommendModule("driveI0:sum.Capacity >=5 where Speed=5400", "driveI1:sum.Capacity >=5 where Speed=5400",
                "cpu:sum.Memory >=512 where CoreNum=4");
        printSimpleSolutions();
        // 变化点5-2：要求为保持输入的简洁见，如果输出多个实例，后面的需要加上实名名称I1
        assertSoluContain(1, "cpu2(Q:2,H:0,S:1),md1(Q:5,H:0,S:1),sd1(0*),md1_I1(Q:5,H:0,S:1),sd1_I1(0*)");
        assertSoluContain("cpu2(Q:20,H:0,S:1),I0{md1(Q:5,H:0,S:1),sd1(0*)},I2{md1_I1(Q:5,H:0,S:1),sd1_I1(0*)}");//方案1： 
    }


 
 
 要点3：
 引擎增加的接口
 https://github.com/goflyboy/jmixconfigengine/commit/d38dd4d3dc79eb07ad3dbb856cc3d78fe1b055b7#diff-eb0e8ec8e8ae861cd14a4a782fac6853d8c43dee604cf96f447836bfb66d4c80
 
 业务上再增加一个字段，使用阶段？ 
 ---然后
 
 规则上增加一个
 1、 在CodeRuleAnno等、Rule类增加
CalcStage   calcStage = CalcStage.MID

  
 enum CalcStage{
	PRE(10),//前置计算，根据输入变量，主要是对控制参数直接赋值方式，要确保所有的控制参数是有值的（针对的输入）（约束求解的赋值），输出控制变量值（是否输入）
	MID(20);//中计算，根据控制变量和用户的输入，使用约束求解进行计算，输出多个解
	POST(30);//后计算，对约束求解的结果（每个解）调用后计算算法进行补充计算 
 }
   
2、在IModule增加
    List<Rule> getAllRules(CalcStage calcStage); 
3、在IOnto增加
     List<Rule> getRules(CalcStage calcStage); 
 
 增加前置计算，如果有输入，就有默认值？ 
 --
 --控制参数，如果有输入（要很少？） ，用于少量复杂的场景（写大量算法的场景） ，尽量不用控制参数（默认都不是控制参数）
 --对一个Rule来说
 
 
 TODO：需要单独抽取controlParameter
 
 --
 
 
 建议参考 Module、ModuleBase、PartCategory三者的关系（如getParts表示获取当前层，getAllParts表示获取所有的），重构ConstraintAlgImpl
 
 
 1、原有的 ConstraintAlgImpl改名为：ModuleAlgImpl
 2、对ModuleAlgImpl进行抽取形成ModuleBaseAlgImpl
 3、新增PartCategoryAlgImpl 
 4、然后基于这个层次结构来实现 sum4Parts
 
   private List<String> parseExpect(String expectStr) {
        // expectStr = "cpu2(Q:20,H:0,S:1),md1(Q:5,H:0,S:1),sd1(0*)"
        // 结果为：["cpu2(Q:20,H:0,S:1)","md1(Q:5,H:0,S:1)","sd1(0*)"
        // 根据“),”来拆分
     
	 }
 
 ConstraintAlgImpl
 
 ConstraintAlgImpl
 
 
 PartAnno
 
 增加了instCode属性？
 
 根据注解的instCodes对PartCategory进行可能，构造出多实例
 
 在PartAnno添加了instCode属性，请修改数据构造的部分，的
 
 2、在ModuleGenneratorByAnno的函数buildModule增加
 修改前:
   buildModule(...) {
    ...
	module.addParts(...);
	module.init();
	....
   }
   createPartCategory(PartAnno partAnno,String code) 。。。{
       ...
	   part.setScope(partAnno.scope());
   }
 修改后:
   buildModule(...) {
    ...
	module.addParts(...);
	MultiInstCategoryUtils.processInstCategory(module);
	module.init();
	....
   }  
 
   createPartCategory(PartAnno partAnno,String code) 。。。{
       ...
	   part.setScope(partAnno.scope());
	   MultiInstCategoryUtils.setInstcodes(partAnno,part);
   } 

 3、在com.jmix.tool.bbuilder增加类MultiInstCategoryUtils
 
class MultiInstCategoryUtils {
	 public static String EX_INST_CODES = "EX_INST_CODES";


	 void processInstCategory(Module module) {
		dstPartCategorys = ...;
		for(partCategory: module.partCategorys) {
			instCodeStr = partCategory.getExtAttr(EX_INST_CODES)
			if(instCodeStr 空）{//没有多实例,直接添加
				dstPartCategorys.add(partCategory);
				continue;
			}
			//有多实例，则安instCode进行复杂
			int codeIndex = 0;//区别实例Id，从0到n是连续的
			List<InstCode> instCodes = parseInstCode(instCodeStr);
			for(instCode:instCodes) {
				//如果instCode.instCode ！= partCategory.code, 抛异常，并打印
				//判断codeIndex == instCode.instId（要求InstId从0开始是连续的)z,抛异常，打印 codeIndex =、++
				//codeIndex 最多有9个实例，超过，则报错
				dstPartCategorys.add(deepClone(partCategory,instCode.instCode);
			} 
		}
		module.setPartCategorys(dstPartCategorys);//重新设置
	 }
	 
	 PartCategory deepClone(PartCategory org, String newCode) {
		dst = org.clone();
		dst.code = newCode;
		克隆dst.atomicParts到dst，并将每个fatherCode设置为newCode 
		参考atomicParts 克隆dst.paras、 rules、dynAttr、dynAttrSchemas
	 }
	 List<InstCode> parseInstCode(String instCodeStr) {
		//解析生成InstCode，例如：instCodeStr="driveI0,driveI1",返回2个InstCode；
		//如果解析过程中心不符合规则，则抛异常，并打印（要求InstId从0开始是连续的)
	 }
	 void setInstcodes(PartAnno partAnno,Part part){
		part.setExtAttr(EX_INST_CODES,partAnno.instCodes());
	 }
	 
	 static class InstCode {
		String instCode;//如:driveI0,driveI1
		String code ;//如:drive
		String instName; //如:I0,I1
		String instId;//0,1 
	 }
 }
 
 
 ConstraintAlgImpl重构的逻辑
 
 
 1、请参考下列设计对 ModuleBaseAlgImpl、ModuleAlgImpl、PartCategoryAlgImpl 进行增强
 
 class  ModuleBaseAlgImpl {
	  protected ModuleBase module;//基础数据（当前执行所需要的） 
	  protected AlgCPModel model;//约束模型
	  
	  //配置领域的模型
	  private Map<String,PartVar> partMap;
	  private Map<String,ParaVar> paraMap;
	    /**
     * 所有规则方法的映射表，存储规则代码到对应方法的映射
     */
    protected Map<String, Method> allRuleMethods = new HashMap<>();
	   
	  	 public void init(AlgCPModel model, IModule module,List<ParConstraint> partConstraintFromReqs) {
			//Module级别
			this.model = model;
			this.module = module;
			
			  // 初始化兼容性约束算法实例
			this.compatibleConstraintAlg = new CompatibleConstraintAlg(model);
		   initModelAfter(model);
		   
		   buildRuleMethods();
		   initAll();
						// 设置输入变量（如有）
			setInputVariables(partConstraints);
			
			setDefaultVisibilityConstraints();//TODO delete 
	 }	
		private void initAll() {
			//针对module.paras，调用initPara创建ParaVar,并放到paraMap
			//参考paras，也对parts进行构建
		}
		
		public void initRule(Method<String,Method> allRuleMethods) {
			//选择要执行的rules
			rules = module.getRule();
			for(rule: rules) {
				rulelMethod=allRuleMethods.get(rule.code):
				executeRuleMethod(rule.code, method);executeRuleMethod(rule.code, method);
			}
		}
		 
	protected ParaVar initParaVar(Para para) { 
        ParaVar paraVar = new ParaVar();
        paraVar.setBase(para);
        if (para.getAssignType() == AssignType.INPUT) {
            registerVar(code, paraVar);
            return paraVar;
        }

        switch (para.getParaType()) {
            case INTEGER:
                paraVar.setValue(newIntVar(Integer.parseInt(para.getMinValue()), Integer.parseInt(para.getMaxValue()),
                        f(ParaVar.VALUE_PATTERN, code)));
                break;
            case ENUM:
                List<DynamicAttributerOption> options = para.getOptions();
                if (options == null || options.isEmpty()) {
                    log.error("Para options not found for code: {}", code);
                    throw new AlgLoaderException("Para options not found for code: " + code);
                }
                paraVar.setValue(newIntVarFromDomain(para.getOptionIds(), f(ParaVar.VALUE_PATTERN, code)));

                for (DynamicAttributerOption option : options) {
                    ParaOptionVar optionVar = createParaOptionVar(para.getCode(), option.getCode());
                    paraVar.getOptionSelectVars().put(option.getCodeId(), optionVar);
                }
                paraVar.getOptionSelectVars().forEach((optionId, optionVar) -> {
                    model.addEquality(paraVar.getValue(), optionId).onlyEnforceIf(optionVar.getIsSelectedVar());
                    model.addDifferent(paraVar.getValue(), optionId)
                            .onlyEnforceIf(optionVar.getIsSelectedVar().not());
                });
                break;
            default:
                log.error("Para type not supported: {}", para.getParaType());
                throw new AlgLoaderException("Para type not supported: " + para.getParaType());
        }
        paraVar.setIsHidden(newBoolVar(f(ParaVar.HIDDEN_PATTERN, code)));
        registerVar(code, paraVar);
        return paraVar;
    }
	    protected PartVar initParaVar(Part para) { 
			 ParaVar paraVar = new ParaVar();
			paraVar.setBase(para);
			if (para.getAssignType() == AssignType.INPUT) { 
				return paraVar;
			}

			switch (para.getParaType()) {
				case INTEGER:
					paraVar.setValue(newIntVar(Integer.parseInt(para.getMinValue()), Integer.parseInt(para.getMaxValue()),
							f(ParaVar.VALUE_PATTERN, code)));
					break;
				case ENUM:
					List<DynamicAttributerOption> options = para.getOptions();
					if (options == null || options.isEmpty()) {
						log.error("Para options not found for code: {}", code);
						throw new AlgLoaderException("Para options not found for code: " + code);
					}
					paraVar.setValue(newIntVarFromDomain(para.getOptionIds(), f(ParaVar.VALUE_PATTERN, code)));

					for (DynamicAttributerOption option : options) {
						ParaOptionVar optionVar = createParaOptionVar(para.getCode(), option.getCode());
						paraVar.getOptionSelectVars().put(option.getCodeId(), optionVar);
					}
					paraVar.getOptionSelectVars().forEach((optionId, optionVar) -> {
						model.addEquality(paraVar.getValue(), optionId).onlyEnforceIf(optionVar.getIsSelectedVar());
						model.addDifferent(paraVar.getValue(), optionId)
								.onlyEnforceIf(optionVar.getIsSelectedVar().not());
					});
					break;
				default:
					log.error("Para type not supported: {}", para.getParaType());
					throw new AlgLoaderException("Para type not supported: " + para.getParaType());
			}
			paraVar.setIsHidden(newBoolVar(f(ParaVar.HIDDEN_PATTERN, code))); 
        return paraVar;
	}
	    protected PartVar initPartCategoryVar(Part para) { //长期好像不需要和PartCategory重复了 TODO
			        PartCategoryVar partCategoryVar = new PartCategoryVar();
				partCategoryVar.setBase(categoryPart);
		}	
		
 }
 
 class ModuleAlgImpl extends{
   private Map<String,PartCategoryAlgImpl> paraCategorys;
 
   public Module getModule() {
		return (Module)super.getModule();
   }
   	 public void init(AlgCPModel model, IModule module,List<ParConstraint> partConstraintFromReqs) {
		//按partCategoryCode,对  partConstraintFromReqs进行分组 partConstraintFromReqMap:Map<String,List<ParConstraint>> 
	 
		//Module，对本层进行初始化
		super.init(model,module,partConstraintFromReqs);
		
		Module bModule = getModule();
		//多Module的每个分类进行初始化
		for(PartCategory partCategory: bModule.getPartCategorys()) {
		   pc4PartConstraintFromReqs = partConstraintFromReqs.get(partCategory.getCode());
		   PartCategoryAlgImpl pcAlg = new PartCategoryAlgImpl();
		   pcAlg.init(model,partCategory,pc4PartConstraintFromReqs);
		}
		
		
		//调用 writeBackToFields， 
		
		//获取buildAllRuleMethods ->allRules
		
		
		//执行规则
			for(PartCategoryAlgImpl partCategoryAlgImpl: paraCategorys...) { 
			   partCategoryAlgImpl.initRule(allRules);
		}
	 }	
	 
	  private void writeBackToFields() throws AlgLoaderException {
        Map<String, Field> fieldMap = getAllFieldVariables();
      
		partVars  = getAllPartVars();
		for(PartVar partVar：partVars） {
		        tVar = newParaVar((ParaVar) v);
                setVariableField(tVar, fieldMap.get(code));
		}
		     
		paraVrs  = getAllParaVars();
		for(ParaVar paraVar：partVars） {
		        tVar = newParaVar((ParaVar) v);
                setVariableField(tVar, fieldMap.get(code));
		} 
    }
	 private  Map<String, Method>   buildAllRuleMethods(Module  module) {
        if (module == null || module.getAllRules() == null) {
            return xx;
        }

        // 获取当前类的所有方法
        Method[] methods = this.getClass().getDeclaredMethods();
        Map<String, Method> allMethods = new HashMap<>();

        // 构建方法名到Method对象的映射
        for (Method method : methods) {
            allMethods.put(method.getName(), method);
        }

        // 根据module.getRules来构建ruleMethods
        for (Rule rule : module.getAllRules()) {
            String ruleCode = rule.getCode();
            Method method = allMethods.get(ruleCode);
            if (method != null) {
                ruleMethods.put(ruleCode, method);
                log.info("Built rule method mapping: {} -> {}", ruleCode, method.getName());

                // 检查rule.ruleSchemaTypeFullName是否为PriorityRule，如果是则构建优先级约束
                if (RuleTypeConstants.isPriorityRule(rule.getRuleSchemaTypeFullName())) {
                    buildPriorityConstraint(rule);
                }
            } else {
                log.error("Rule method not found for rule code: {}", ruleCode);
                throw new AlgLoaderException("Rule method not found for rule code: " + ruleCode);
            }
        }

        log.info("Built {} rule methods from module rules", ruleMethods.size());
		return allMethods;
    }

 }
 
 class PartCategoryAlgImpl extends{
    public PartCategory getModule() {
		return (PartCategory)super.getModule();
   }
   
   	 public void init(AlgCPModel model, IModule module,List<ParConstraint> partConstraintFromReqs) {
		//Module级别
		

	 }	
 }
 
 
     public void initModel(AlgCPModel model, IModule module, boolean isAttachRelax, List<RelaxVar> confictedRelaxs,
            List<ParConstraint> partConstraintFromReqs) {
			
	 public void init(AlgCPModel model, IModule module,List<ParConstraint> partConstraintFromReqs) {
		//Module级别
		

	 }			
			
			
		ParConstraint	

public class PartConstraintReq {



}

	
1. 	需要适配成我们自己的 : 
--
		
		
		
 PartCategoryAlgImpl--支持多实例的场景？
 
 
 
 //多实例的问题--**
 
 TODO： 平铺更直观一点--更好
 外面的算法文件，通过fatherCode来标识不同点
 
 
 
 //生成一个动态的规则
 drive.SumCapacity = driveI1.SumCapacity + driveI2.SumCapacity 
 
 多实例
 
 开源的代码：
 1、*Module* -->*Product*
 2、*Config->*Inst  
 
 
 
 
 
 TODO：
 1、多实例，类似数量，有所有的总数据分类总数量，单次数量的要求？—**  前端输入要有明确？

 1、引擎的解耦性设计？ 所有的用例需要调整？ 
 2、
 3、仅计算某一个分类，是不是不存在？--**，只筛选？
？
  2、潜在问题，变量过度 （也就是没有要求，也初始了，可能导致，解过的）
   见到多实例，如果没有明确要求，则过滤掉
 代码优化：
     private static Para clonePara(Para original) {
        Para copy = new Para();
		
		    protected Set<String> codesOfHiddenConstraint = new HashSet<>();
		
2、PartConstraintReq 这个也需要改？ ---PartConstraintReq --多实例？ 



    /**
     * 变量映射表，存储所有创建的变量实例
     */
    protected Map<String, Var<?>> varMap = new LinkedHashMap<>();
3、
--Product  

PartCategoryVar 这个注解不需要
 class PartCategory {


}


2026-4-12：
1、是否要支持动态多实例？ ---


--drive   pcSumQantity
----driveI1    pcQuantity 
------part1.Q
------part2.Q 
----driveI2    pcQuantity 
------part1.Q
------part2.Q 


drive.PCQuantity();
drive.PCSumQuantity();/




pcQuantity
pcSumQantity


public class RefProgObjSchema {

    /**
     * 参数类型常量
     */
    public static final String PROG_OBJ_TYPE_PARA = "Para";

    /**
     * 部件类型常量
     */
    public static final String PROG_OBJ_TYPE_PART = "Part";

    /**
     * 部件类型常量
     */
    public static final String PROG_OBJ_TYPE_PARTCATEGORY = "PartCategory";
	
	
    /**
     * 可编程对象类型
     */
    private String progObjType;

    /**
     * 可编程对象编码
     */
    private String progObjCode;

    /**
     * 可编程对象属性
     */
    private String progObjField;
}
    public List<RefProgObjSchema> getToRightProgObjs() {
	
	
	

getFromLeftProgObjs 

getFromRightProgObjs

TODO:三元的怎么表达？ --算法的统一在看一下？

 


大模型：

 1、 在PartAnno等、PartCategory类增加
boolean supportMultiInst = false;//默认值不支持

2、  在ModuleGenneratorByAnno添加这个处理，所有的处理都放在MultiInstCategoryUtils


1、增加Cardinality 举
//基数
enum Cardinality{
	ONE(10),//1:1，N:1中1
	MANY(20),//1:N，M：N中的N（*）
}
 

2、在类Rule、CodeRuleAnno等添加属性
  Cardinality leftCardinality = Cardinality.ONE;
  Cardinality rightCardinality = Cardinality.ONE;
  EffectScope effectScope = EffectScope.SingleInst;

 
2、在ModuleGenneratorByAnno中增加，做相应添加和赋值
2.1  
leftProObjsStr = "drive:Select|Quantity"  ,解析为：
RefProgObjSchema
--progObjType="PartCategory"
--progObjCode="drive"
--progObjCode="Select"
--progObjCode="Quantity"

解析的结构设置到rule的getToRightProgObjs
rule.rawCode.setFromLeftProgObjs(...)


2.2.  增加对leftCardinality，rightCardinality和effectScope的赋值

 


class MultiInstPartCategoryAlgImpl
{
  List<PartCategory> partCategorys;
  
  
}


   if (drive.getSumSumParaByAttr("Capacity").getIsHasInputed()) {
   
   
1、添加下列类
class AttrPara {
	String attrCode;
	AttrParaType type = AttrParaType.SUM; 
}
enum AttrParaType {
	Sum(10), //对每个分类实例的汇总（考虑到的单选和多选）
	SumSum(20),//对每个分类所有的实例值汇总，对应输入多实例
}

2、在Onto类上增加 
List<AttrPara>  attrParas = new ArrayList();
 
3、在PriorityRuleAnno、CodeRuleAnno等注解上增加
String attrParaCodes = "";

4、在ModuleGenneratorByAnno中增加，做相应添加和赋值
attrParaCodes = "Capacity:SumSum,Quantity:SumSum")


5、在类ModuleBaseAlgImpl类中
修改前：
 protected void initAll(IModuleAlg moduleAlgFile) {
...
...
}
修改后：
 protected void 	, AttrParaType aType) {
...
 newAttrParaVar(module.getAttrParas());
...
}
void newAttrParaVar(List<AttrPara> attrParas) {
	for(attrPara:attrParas) { 
		newAttrParaVar（attrPara)
	}
}

void newAttrParaVar(AttrPara attrPara) {
				Para para = new Para(); // 没有创建，则动态创建这个变量
                para.setCode(attrPara.getAttrCode());
                para.setAssignType(AssignType.INPUT);
                // 从对应的属性里面获取数据issue（min，max等）
                para.setParaType(ParaType.INTEGER);
                ModuleBase tempModule = (ModuleBase) module;
                tempModule.addPara(para);
                pVar = initParaVar(para);
				 paraMap.put(paraCode, pVar);
}

1、在PartCategoryAlgImpl增加下列两个函数
interface PartCategoryAlgImpl {

	com.jmix.executor.impl.algmodel.ParaVar getSumSumParaByAttr(String attrCode);
	com.jmix.executor.impl.algmodel.ParaVar getSumParaByAttr(String attrCode);
	
	
}


2、 在类ModuleBaseAlgImpl类实现下列两个方法
	com.jmix.executor.impl.algmodel.ParaVar getSumSumParaByAttr(String attrCode){
		return paraMap.get(attrCode);//找不到打印并报错
	}
	com.jmix.executor.impl.algmodel.ParaVar getSumParaByAttr(String attrCode){
		return paraMap.get(attrCode);//找不到打印并报错
	}

3、对 ConstraintAlgImplTestBase.PartCategoryVar
修改前：
class PartCategoryVar ... {
....

}

修改后： 
class PartCategoryVar ... {
....
      ModuleBaseAlgImpl  algImpl;
	 ParaVar getSumSumParaByAttr(String attrCode){
		com.jmix.executor.impl.algmodel.ParaVar tmpVar = algImpl.getSumSumParaByAttr();
		return toParaVar(tmpVar)
	}
	 ParaVar getSumParaByAttr(String attrCode){
		com.jmix.executor.impl.algmodel.ParaVar tmpVar = algImpl.getSumParaByAttr();
		return toParaVar(tmpVar)
	}
}



}
3、在类ConstraintAlgImplTestBase中
3.1 
对方法抽取一个静态方法：
    protected Var<?> newParaVar(com.jmix.executor.impl.algmodel.ParaVar internalParaVar) {  
	--->
	
        public static  Para toParaVar(com.jmix.executor.impl.algmodel.ParaVar internalParaVar) {
			.....
		}


3.2 
修改前：
    protected void afterInitParas(IModule module, IModuleAlg moduleAlg) {
      
    }
	
修改后：
    protected void afterInitParas(IModule module, IModuleAlg moduleAlg) {
	   partCategoryAlgImpls = this.getPartCategoryAlgImpls();
	   获取当前类的字段（类似：private PartCategoryVar drive）filedMap
	   for(partCategoryAlgImpl:partCategoryAlgImpls) {
		    Filed partCategoryField = filedMap.get(partCategoryAlgImpl.getCategorycode());
			根据partCategoryAlgImpl创一个PartCategoryVar partCategoryVar
			将partCategoryVar设置到partCategoryField上
	   }
	   
      
    }
         

TODO：输入值的完整性问题？**
   protected void afterSetInputVariable() {
        // 设置 总总 和 总变量的关系
        List<AttrPara> sumSumAttrParas = getMultiInstPartCategoryInput().getSumSumAttrParas();
        for (AttrPara attrPara : sumSumAttrParas) {
            // 有一个已经输入，则汇总也设置数据
            // 如果汇总已经输入，一输入的为准
            // 总总参，和总参的关系，后续支持写逻辑（多个总总之间issue）

        }
		
		
		    public void testQueryWithInstanceAttribute() {
        PartConstraintReq req = new PartConstraintReq();
        req.setPartCategoryCode("drive");
        req.setAttrCode("sum.Capacity");  
		
		Sum.Cap

TODO： setInputVariables


TODO: 输入的优化？


TODO： addPartConstraintReqs
  
 
		
		
1、在PartCategoryVar添加：
ParaVar getSumSumParaByAttr(String attrCode) {
	....
}
 
 enum CalcStage{
	PRE(10),//前置计算，根据输入变量，主要是对控制参数直接赋值方式，要确保所有的控制参数是有值的（针对的输入）（约束求解的赋值），输出控制变量值（是否输入）
	MID(20);//中计算，根据控制变量和用户的输入，使用约束求解进行计算，输出多个解
	POST(30);//后计算，对约束求解的结果（每个解）调用后计算算法进行补充计算 
 }
   
2、在IModule增加
    List<Rule> getAllRules(CalcStage calcStage); 
3、在IOnto增加
     List<Rule> getRules(CalcStage calcStage); 
 
 
 
 partVar.getInstId() == 1 &&partVar.getCode().equals("sd1")
 场景1：仅有过滤条件，如：
	输入：attrExpr = “where Speed=5400”
	输出结果：req.attrWhereCondition="Speed=5400" ,req.attrComparator=null,...
 场景2：仅有汇总条件，如：
 	输入：attrExpr = “ Sum_Capacity >=5”
	输出结果：req.attrWhereCondition=null ,req.attrComparator=">=",...
 场景3：同时有汇总条件和过滤条件
 	输入：attrExpr = “  Sum_Capacity >=5 where Speed=5400”
	输出结果：req.attrWhereCondition="Speed=5400" ,req.attrComparator=">=",...
 
 2026-4-17：
 1、核心要点:
 ---引擎的版本，数据的版本？ 要配套？***
 
 2026-4-19：
 
 2、怎么快速新增算法模式？
 