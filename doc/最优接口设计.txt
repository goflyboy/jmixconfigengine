前后端接口采用x、

2026-1-2:
1、分阶段生成的方案思路，prompt的拆分
2、
1、代码要合在一起？ 
2、基于本体的logic的逻辑？
3、

4、怎么让claude生成的代码的OK的
5、基于本体
4、洪利总的skills的学习？***

6、多维求解的功能  
7、混合求解的功能？**
8、把我开发的代码，然后合入到这个分支上去，可以包统一（代码结构基本一致），这样比较方便--使用claude的功能，搞一个skill
9、OAG的能力



更长远的未来
1、基于本体的IT系统架构？****
 
实例属性定义  --单个，多个


54000
实例1、实例2

select sum(容量) where 容量 contains("5400“） 单位相加


关键配置逻辑：
1、固态硬盘必须配置同一种，并且最多配置2块  
2、优先使用固态硬盘，固态硬盘满配后还不满足要求的用机械硬盘补充
3、用户提了固态硬盘数量的，使用固态硬盘数量，否则需要计算固态硬盘数量
4、固态硬盘优先匹配高速率容量，用机械硬盘增配低速率容量
5、不能使用完全不包含用户需要的速率的固态硬盘做配置


TODO：这种能否独立测试？--独立搞？ 看一下? 


 
Drive
SolidDrive
MechDrive

设计1：
请按下列设计的实现，完成对于测试用例设计，并调通，并远程提交

##详细实现
1、增加类
class ExtSchema {
	public final String IntegerUnit = "IntegerUnit";////带单位的整数值,单位，codeValue是Integer 如GB/S如: Speed_5400 
		public final String UNIT = "unit";  
	public final String StringUnit = "StringUnit";//单位,codeValue是String 
	 
}
@Data
class  InstanceDynAttrValue {//多实例类型属性值 
	int instId; 
    private Map<String, String> instAttr = new HashMap<>(); //

	toJsonString (InstanceDynAttrValue iValue)-->String {....}
	fromJsonString(String strValue) -->InstanceDynAttrValue{....}
}


2、新增类PartCategory
class PartCategory extends Part { 
}


2、DynamicAttribute增加属性 String optionExtSchema  

 

3、参考ParaAnno定义动态属性的注解 DAttrAnno1、DAttrAnno2、DAttrAnno3、DAttrAnno4、DAttrAnno5、DAttrAnno11、DAttrAnno12、DAttrAnno13
 根据DAttrAnno*给PartCategory.dynAttrSchemas生成数据 

 参考案例：表示给 drive这个 PartCatagory生成3条dynAttrSchema
@DAttrAnno1(code="BrandWidth",optionExtSchema=ExtSchema.IntegerUnit, options = { "BW_8GB:8:GB/S", "BW_16GB:16:GB/S"}    
@DAttrAnno2(code="Speed",optionExtSchema=ExtSchema.StringUnit,options = { "Speed_5400:5400:转", "Speed_9000:9000:转", "Speed_7200a5400:7200/5400:转",instType=0}   
@DAttrAnno3(code="Capacity",optionExtSchema=Attr.;,options = { "Capacity_8T,8:T", "Capacity_16T,16:T", Capacity_32T,32:T",instType=0}   
    private PartCategoryVar driveVar;

 
4、新增DAttrInherit注解类
案例如下：
下列案例表示  solid PartCategory的dynAttrSchema属性，继承（先复制方式实现） drive PartCatagory属性，并对Speed及Capacity的instType属性进行了重写

@DAttrInherit(fatherCode="driveVar",overrideAttrs={"Speed:instType=1","Capacity:instType=1"});	//继承 driveVar的属性定义，同时对Speed和Capacity进行重写
@DAttrAnno11(code="maxCapacity", optionExtSchema=Attr.IntegerUnit, options = { "MaxCapacity_8T:8:T", "MaxCapacity_16T:16:T"}   
    private PartCategoryVar solidDrive;

5、新增DAttrAnno*
 根据DAttrAnno*给PartCategory.attrs


5、新增PartCategoryVar类
class  PartCategoryVar  extends PartVar {}

6、PartAnno新增
 
    /**
     * 实例1规格属性
     */
    String[] attrsInst1() default {};
    /**
     * 实例2规格属性
     */
    String[] attrsInst2() default {};
    /**
     * 实例3规格属性
     */
    String[] attrsInst3() default {};
    /**
     * 实例4规格属性
     */
    String[] attrsInst34() default {};

7、在ModuleScenarioTestBase增加对应的生成逻辑
7.1 根据DAttrAnno*给PartCategory.dynAttrSchemas生成数据 
7.2 根据DAttrInherit复制和修改设计，详细参考4
7.3 根据ParatAnno新增的attrsInst*生成对应的InstanceDynAttrValue，然后toJSONString存入到 attrs中

8、参考 com.jmix.scenario.hello.HelloConstraint生成一个ComputerConstraint类（主要是生成数据的，不需要生成规则）
  增加com.jmix.scenario.partcatgorypackage
  需要生成一个简单的测试用例
##样例数据
####样例数据案例： 
--电脑  --产品
---硬盘  --PartCategory1
------固态硬盘			数量			接口带宽	 	支持转速容量分组  --PartCategory11
-----------固态硬盘1					8GB/S	    [{”实例分组“：1，“转速”：“5400”，“容量”：“2T”}，{”实例分组“：2，“转速”：“7200/5400”，“容量”：“1T”}]
-----------固态硬盘2					8GB/S	    [{”实例分组“：1，“转速”：“7200/5400”，“容量”：“4T”}]
-----------固态硬盘3					8GB/S	    [{”实例分组“：1，“转速”：“9000”，“容量”：“4T”}]
------机械硬盘			数量			支持转速容量分组 --PartCategory12
-----------机械硬盘1					 { “转速”：“5400”，“容量”：“2T”} 
-----------机械硬盘2					 { “转速”：“7200”，“容量”：“2T”} 
-----------机械硬盘3					 [ “转速”：“9000”，“容量”：“2T”} 
####样例数据对应的注解表现形式
@DAttrInherit(fatherCode="driveVar",overrideAttrs={"Speed:instType=1","Capacity:instType=1"});	//继承 driveVar的属性定义，同时对Speed和Capacity进行重写
@DAttrAnno11(code="maxCapacity", optionExtSchema=Attr.IntegerUnit, options = { "MaxCapacity_8T:8:T", "MaxCapacity_16T:16:T"}   
    private PartCategoryVar solidDrive;


@DAttrAnno1(code="BrandWidth",optionExtSchema=ExtSchema.IntegerUnit, options = { "BW_8GB:8:GB/S", "BW_16GB:16:GB/S"}    
@DAttrAnno2(code="Speed",optionExtSchema=ExtSchema.StringUnit,options = { "Speed_5400:5400:转", "Speed_9000:9000:转", "Speed_7200a5400:7200/5400:转",instType=0}   
@DAttrAnno3(code="Capacity",optionExtSchema=ExtSchema.IntegerUnit;,options = { "Capacity_8T,8:T", "Capacity_16T,16:T", Capacity_32T,32:T",instType=0}   
    private PartCategoryVar driveVar;

@DAttrInherit(fatherCode="driveVar",overrideAttrs={"Speed:instType=1","Capacity:instType=1"});	//继承 driveVar的属性定义，同时对Speed和Capacity进行重写
@DAttrAnno11(code="maxCapacity", optionExtSchema=Attr.IntegerUnit, options = { "MaxCapacity_8T:8:T", "MaxCapacity_16T:16:T"}   
    private PartCategoryVar solidDrive;
 


@PartAnno(fatherCode="solidDrive", attrs={"BW_8GB","MaxCapacity_8T"}
	,attrsInst1={"Speed_5400","Capacity_8T"},
	,attrsInst2= {"Speed_7200a5400","Capacity_8T"})  
    private PartVar solidDrive1;
 
@PartAnno(fatherCode="solidDrive", attrs={"BW_16GB","MaxCapacity_16T"}
	,attrsInst1={"Speed_9000","Capacity_16T"},
	,attrsInst2= {"Speed_7200a5400","Capacity_16T"})  
    private PartVar solidDrive2;  

@DAttrInherit(fatherCode="driveVar");	  
    private PartCategoryVar mechDrive;
@PartAnno(fatherCode="mechDrive", attrs={"BW_8GB","Speed_5400","Capacity_8T"}
    private PartVar mechDrive1;
@PartAnno(fatherCode="mechDrive", attrs={"BW_16GB","Speed_7200a5400","Capacity_16T"}
    private PartVar mechDrive2;

 



设计22222：




输入：
要求5400速率的固态硬盘2块，要求5400速率的硬盘容量>=5T， 



请按下列设计的实现，完成对于测试用例设计，并调通，并远程提交
 
0、Part增加下列方法
 @JsonIgnore
InstanceDynAttrValue  getInstanceAttrs() {
	String InstanceAttrsStr = this.getAttr("instanceAttrs"); 
	if(InstanceAttrsStr == null || InstanceAttrsStr.empty()) { return null;}
	return InstanceDynAttrValue.fromJsonString(InstanceAttrsStr);
}
@Ignore
Part clone() {
	Part  pc = new Part();
	//请其都要复制 //TODO 复制性能是否好，多实例的情况怎么解决？**
} 

//先过滤  
1、对PartCategory做下列增强
 
@Ignore
Map<String,PartCategory>  partCategoryMap = new HashMap();

@Ignore
Map<String, Part> partMap= new HashMap();

@Ignore
PartCategory clone() {
	PartCategory  pc = new PartCategory();
	//除了parts/partCategoryMap/partCategory,请其都要复制
}

@Ignore
public void init(..) (
	对partCategoryMap、partMap进行初始化
}
List<PartCategory> query(  PartConstraintReq constraintReq){
	return query(this,constraintReq);
}	

//返回满足条件的Part 
List<PartCategory> query(PartCategory category,PartConstraintReq constraintReq){ 

	 Optional<FilterCondition> filterCondition = FilterExpressionExecutor.parseFilterExpression(constraintReq.attrWhereCondition);
	 if(filterCondition.get() == null) {
		抛出异常
	 }
	 return query(category,constraintReq,attrWhereCondition);
}

List<PartCategory> query(PartCategory category,PartConstraintReq constraintReq,FilterCondition attrWhereCondition ){ 
{
	 PartCategory resultPartCategory = category.clone();
	 List<PartCategory> resultSubPartCategory = new ArrayList();
	 if(!category.partCategoryMap.isEmpty()) {
		for(PartCategory pc: partCategoryMap.values()) {
			resultSubPartCategory.addAll(query(pc,constraintReq);
		}  
	 }	
	 resultPartCategory.addPartCategory(resultSubPartCategory);

	 //处理当前层part 
	 result = parseAttribute(constraintReq.attrCode,category.dynAttrSchemas), 解析出原始属性Capacity    
	 List<Part> filterParts = new ArrayList();
	 if(result.first.instType == 0) {//非实例属性（默认）
		filterParts = FilterExpressionExecutor.doSelect(category.partMap.values(),attrWhereCondition);  
	 }
	 else {//实例属性 
		for(Part part:	category.partMap.values()) {
			List<InstanceDynAttrValueItem> instValues = FilterExpressionExecutor.doSelect(part.getInstanceAttrs().getInstsValues(),attrWhereCondition);
			int sumAttrValue = 0;
			for(InstanceDynAttrValueItem instValue: instValues) {
					sumAttrValue += parseInt(instValue.getInstAttr(result.first.getCode())) //TODO类型处理
			}
		    Part cPart = part.clone();
	        cpart.addAttr(result.first.getCode(),String.valueOf(sumAttrValue));
			filterParts.add(cPart);
		} 
	 }
	 resultPartCategory.addPart(filterParts); 
}
 
Pair<DynamicAttribute,String>  parseAttribute(String orgAttrCode,List<DynamicAttribute> dynAttrSchemas){
	....
	//例如：orgAttrCode="sum.Capacity"， 输出：DynamicAttribute{code=”Capacity“，...}, AttrFunConstant.SUM 
}

2、Module的类中init方法，增加对Part的初始化
修改前：
void init() {
	....
    initRefRelationGraph();
}
修改后：
void init() {
	....
    initRefRelationGraph();
	 根据fatherCode进行对PartCategory的partCategoryMap partMap 进行初始化
}
 
 

3、com.jmix.executor.omodel增加下列类：
 PartConstraintReq、PartConstantAttr、AttrFunConstant
 class PartConstraintReq {
	String partCatagory  
	String attrCode 
	String attrComparator 
	String attrValue 
	String attrWhereCondition 
}
class PartConstantAttr{
	public final String Quantity = "Quantity";//动态属性，常量
	 

}
//属性函数常量
class AttrFunConstant{
	public final String FUN_PREFIX_SUM = "sum";
	public final String FUN_PREFIX_AVERAGE = "average"; 
	public final String FUN_PREFIX_EMPTY = ""; 
}
 

 
4、增加PartCategory的测试用例

需要针对 List<PartCategory> query(  PartConstraintReq constraintReq){ 进行测试
需要包含下列输入：


//例如：5400速率的硬盘容量>=5T  
class PartConstraintReq {
	partCatagory = "drive"
	attrCode="sum.Capacity"//多个的汇总
	attrComparator=">="
	attrValue="5" 
	attrWhereCondition="Speed=5400"
}

//例如：单个硬盘的容量只能是2T，单个的约束都放到里面去？ 
//表示1
class PartConstraintReq {
	partCatagory = "drive"
	attrCode="" //单个硬盘
	attrComparator=""
	attrValue="" 
	attrWhereCondition="Capacity=5"
}
 

//例如：5400速率的固态硬盘2块
class PartConstraintReq  {
	partCatagory = "solidDrive"
	attrCode="sum.Quantity"
	attrComparator="=="
	attrValue="2" 
	attrWhereCondition="Speed=5400" 
|


 class PartConstraintReq  {
	partCatagory = "solidDrive"
	attrCode="sum.Quantity"
	attrComparator="=="
	attrValue="2" 
	attrWhereCondition="Speed like %5400%" 
|


 
Drive
SolidDrive
MechDrive

设计3333：
请按下列设计的实现，完成对于测试用例设计，并调通，并远程提交

 

关键配置逻辑：
1、固态硬盘必须配置同一种，并且最多配置2块  
2、优先使用固态硬盘，固态硬盘满配后还不满足要求的用机械硬盘补充
3、用户提了固态硬盘数量的，使用固态硬盘数量，否则需要计算固态硬盘数量
4、固态硬盘优先匹配高速率容量，用机械硬盘增配低速率容量
5、不能使用完全不包含用户需要的速率的固态硬盘做配置

 
多实例、多属性
 

--电脑  --产品
---硬盘  --driverVar
------固态硬盘	--solidDriverVar	 
-----------固态硬盘1	 --solidDriver1Var	.qty  .isSelected   capacityWeight = 100  --5400-2T ，7200/5400-1T
-----------固态硬盘2	 --solidDriver2Var	.qty  .isSelected	capacityWeight = 200  --7200/5400 -4T
-----------固态硬盘3	 --solidDriver3Var	.qty  .isSelected	capacityWeight = 300  --9000	 
------机械硬盘	--mechDriverVar		 
-----------机械硬盘1	--mechDriver1Var	.qty  .isSelected	capacityWeight = 30   5400
-----------机械硬盘2	--mechDriver2Var	.qty  .isSelected	capacityWeight = 20   7200 
-----------机械硬盘3	--mechDriver2Var	.qty  .isSelected   capacityWeight = 10   9000


1、固态硬盘必须配置同一种，并且最多配置2块  

solidDriver1Var.qty <=2  初始:[0,100]  //并且最多配置2块  
solidDriver1Var.isSeleted + solidDriver2Var.isSeleted + solidDriver1Var.isSeleted <=1
if(solidDriver1Var.isSeleted =1) then solidDriver1Var.qty >= 0  




4、固态硬盘优先匹配高速率容量，用机械硬盘增配低速率容量
所以，“固态硬盘优先匹配高速率容量”可能意味着：在满足5400速率需求时，优先使用转速更高的实例分组（即7200/5400，因为它还支持7200，所以是更高速的）来提供容量，当这种高速实例分组不够时，再使用只支持5400的实例分组。

建模步骤：

将固态硬盘的实例分组分为两类：高速组（转速包含7200或9000，且同时包含5400，即7200/5400）和低速组（只包含5400）。注意，根据数据，固态硬盘中没有只包含7200的，所以高速组就是7200/5400，低速组是5400。

优先使用高速组的固态硬盘，当高速组的固态硬盘用尽（达到最大数量）仍不满足容量需求时，再使用低速组的固态硬盘。

当固态硬盘的所有实例分组（高速和低速）都用尽后，仍不满足容量需求，再使用机械硬盘（机械硬盘都是5400）。

因此，我们需要按照以下顺序使用硬盘：
第一优先级：固态硬盘高速组（转速7200/5400）
第二优先级：固态硬盘低速组（转速5400）
第三优先级：机械硬盘（转速5400）

---> capacityWeight  --这个值好像动态的？**TODO：需要固态和机械在一起


solidDriver1Var.isSeleted * solidDriver1Var.capacityWeight + solidDriver2Var.isSeleted * solidDriver2Var.capacityWeight + .... = objCapacityWeight

max(objCapacityWeight)

2、优先使用固态硬盘，固态硬盘满配后还不满足要求的用机械硬盘补充
--满配后？
但是，在约束建模中，我们通常通过条件约束来实现。不过，这里我们可以用另一种方式：通过权重和约束结合。

然而，如果我们已经用权重来体现优先级，那么求解器会自动优先选择固态硬盘，直到固态硬盘不够用时才会选择机械硬盘。
但是，这并不能严格保证“固态硬盘满配后才用机械硬盘”，因为如果固态硬盘数量没有达到上限，但是已经满足容量需求，那么就不会用机械硬盘，这是合理的
--->鉴于需求的模糊性,和其它约束还有冲突？ --“固态硬盘的数量上限是明确的（2块）” 
---TODO：后续要研究，好麻烦  先用4来搞，简化处理


3、用户提了固态硬盘数量的，使用固态硬盘数量，否则需要计算固态硬盘数量
solidDriver1Var.qty == 5



5、不能使用完全不包含用户需要的速率的固态硬盘做配置  
--先过滤和执行能满足


动态：
  要求5400速率的硬盘容量>=5T， 
solidDriver1Var.qty * solidDriver1Var.capacity + solidDriver2Var.qty * solidDriver2Var.capacity + .... = sum >=5T
 要求5400速率的固态硬盘2块，  ==等于2
solidDriver1Var.qty +  solidDriver2Var.qty + 。。。 ==2




设计2：
 
1、对类 PartVar
 1.1 删除subPartSelectedVars
 1.2 增加  
   List<PartVar>  subParts = new ArrayList();
   Map<String,PartVar> subPartMap = ....;//方便搜索
   BoolVar isSelected;//是否选中

 增加类PartCatagoryVar 
  class PartCatagoryVar extends PartVar {

		PartCatagoryVar(...) {
			setQty(null);//不初始化
			setIsHidden(null);//不初始化
		} 

  }
2、对类InferParasReq
  增加 List<PartConstraintReq>  partConstraintReqs


3、在类ModuleScenarioTestBase 
3.1 增加函数
toPartConstraintReqs(String partCatagory, String strReq1,....) {
	///....见下列案例
}

constraintReq1 = "sum.Quantity ==2 where Speed=540";  转化结果为：
//例如：5400速率的固态硬盘2块
class PartConstraintReq1 {
	partCatagory = "solidDrive"
	attrCode="sum.Quantity"
	attrComparator="=="
	attrValue="2" 
	attrWhereCondition="Speed=5400" 
|

constraintReq1 = "sum.Quantity ==2 where Speed like %5400%";   转化结果为：
 class PartConstraintReq  {
	partCatagory = "solidDrive"
	attrCode="sum.Quantity"
	attrComparator="=="
	attrValue="2" 
	attrWhereCondition="Speed like %5400%" 
|
3.3 增加函数
      List<ModuleInst> inferRecommend(String partCode,String partCatagory, String constraintReq1,....) {
		调用toPartConstraintReqs 根据入参构建 req
		//参考inferParas
	  }


4. PartCategory的类

List<PartCategory> query(PartConstraintReq constraintReq) --》返回值改为List<Part>  



5、对类ModuleConstraintExecutorImpl做下列更新
5.1 
   public Result<List<ModuleInst>> inferParas(InferParasReq req) {
        ....
		module.init();
		
		....
	}    Optional<FilterCondition> filterExpression = FilterExpressionExecutor.parseFilterExpression(constraintReq.getAttrWhereCondition());

5.2 在方法initModelByReq增加对PartConstraintReqs的处理


   private void  addPartConstraintReqs(module,req.partConstraintReqs) {
	
		for( PartConstraintReq partConstraintReq :req.partConstraintReqs) {
			PartCategory partCatagory = module.getParaCategory(partConstraintReq.partCatagory);
			List<Part>  filterParts = partCatagory.query(partConstraintReq).getParts();	
			partCatagory.parseAttribute(partConstraintReq.attrCode) ->result 
			根据result.second 构建约束表达式
			如果result.second == AttrFunConstant.FUN_PREFIX_SUM， 
			 alg.sumFunConstaint( filterParts,  result.sumAttrCode, partConstraintReq.attrComparator，partConstraintReq.attrValue); 
				
	}




6、对类ConstraintAlgImpl做下列更新
6.1 现有的   protected PartVar createPartVar(String code)  重构为两个函数：

   protected PartVar createPartVar(String code) {
        Optional<Part> partOpt = module != null ? module.getPart(code) : Optional.empty();
        if (!partOpt.isPresent()) {
            log.error("Part not found for code: {}", code);
            throw new AlgLoaderException("Part not found for code: " + code);
        }
        Part part = partOpt.get();
		if(Part instanceof PartCatagory) {
				partVar = createPartCategoryVar(
		 }
		 partVar = createPartVar(part);
		return partVar;
		
   }
	protected PartVar createPartVar(Part part) {
		//现有代码。。。
    }

    protected PartVar createPartCategoryVar(PartCatagory categoryPart) {
        PartCatagoryVar partCategoryVar = new PartCatagoryVar();
        partVar.setBase(categoryPart);
        for(Part part: categoryPart.partMap.values) {
			createPartVar(part);
		}
		for(PartCatagory tcategoryPart: categoryPart.partCategoryMap.values) {
			createPartCategoryVar(tcategoryPart);
		} 
	}
	

6.2 添加一个sumFunConstaint 
	void sumFunConstaint(List<Part> sumParts, String sumAttrCode, String comparator，int leftValue) {

		下列代码需要根据 Ortools来生成  
			IntVar sumFunVar = ....
			for(Part part: sumParts) {
				PartVar  partVar = getPartVar(part.code);
				sumFunVar = sumFunVar + partVar.qty *  toInt( part.getAttr(sumAttrCode));
			}
			sumFunVar comparator leftValue
	}	


设计5：

TODO：怎么实现差量计算：

方案1： 加载是动态，只加载需要的变量？ ---**  长期 
方案2： 根据过滤的，设置的数量*** --其它设置为0：


请按下列设计增强

1、对ModuleConstraintExecutorImpl做下列增强
1.1 添加一个函数

calcUnFilterLeafParts(PartCatagory allPartCategory,List<Part> filterLeafParts) {
	List<Part> unFilterLeafParts = ...;
	//遍历 allPartCategory本身的parts,及其子PartCategory的parts ，如果 不在filterLeafParts，添加到unFilterLeafParts
	return unFilterLeafParts;
}

1.2 修改函数：addPartConstraintReqs
修改前：
 ... addPartConstraintReqs(...){
	...
		List<Part> filterParts = getAllLeafParts(filteredCategory); 
	...
		alg.sumFunConstraint(....)
	...

}

修改后：
... addPartConstraintReqs(...){
	...
		List<Part> filterParts = getAllLeafParts(filteredCategory);
		List<Part> unFilterParts = calcUnFilterLeafParts(partCategory,filterParts);
	...
		alg.sumFunConstraint(....)
		alg.setPartUnSelected(unFilterParts);
	...

}


2、在类ConstraintAlglmpl添加下列方法

void setPartUnSelected(List<Part> parts) 【
	遍历调用setPartUnSelected

}
 
void setPartUnSelected(Part part) 【
	根据part.code找到对应partVar 
	根据ortools设置
     partVar.isselected == 0
     partVar.qty == 0
}
 


设计6：
请按下列设计增强,保证编译通过，并跑通ComputerOptiSolutionTest，并提交远程
0、对类InferParasReq增加属性
String partCatagoryCode;



1、对类ModuleScenarioTestBase下列方法更新
修改前：strReq="sum.Quantity ==2 where Speed=5400"
    protected List<PartConstraintReq> toPartConstraintReqs(String partCategory, String... strReqs) {
					...
		            PartConstraintReq req = new PartConstraintReq();
					req.setPartCategory(partCategory);
					...
	}

修改后：strReq="sd:sum.Quantity ==2 where Speed=5400"
    protected List<PartConstraintReq> toPartConstraintReqs(String partCategory, String... strReqs) {
					...
		            PartConstraintReq req = new PartConstraintReq();
					按:分割，第一个为reqPartCategory
					req.setPartCategory(reqPartCategory);
					...
	}

3、对类ModuleConstraintExecutorlmpl做下列更新
3.1 方法initModelByReq更新如下：
修改前：
initModelByReq(InferParasReq req, ConstraintAlgImpl alg) {
	...
	addPartConstraintReqs(alg, req.getPartConstraintReqs());
	...
}
修改后：
initModelByReq(InferParasReq req, ConstraintAlgImpl alg) {
	...
	addPartConstraintReqs(alg,req.getPartCatagoryCode(), req.getPartConstraintReqs());
	...
}
3.2 addPartConstraintReqs更新如下：
修改前：
 ... addPartConstraintReqs(...) {
	.... findPartCategory(alg.getModule(), partConstraintReq.getPartCategory());
	....
 }
修改后：
 ... addPartConstraintReqs(...) {
	.... findPartCategory(alg.getModule(),partCatagoryCode);
	....
 }

4、对PartCategory修改如下：
修改前：
  public PartCategory query(PartConstraintReq constraintReq) {
        return query(this, constraintReq);
    }
修改后：
  public PartCategory query(PartConstraintReq constraintReq) {
		在this根据查找code=constraintReq.partCatagoryCode的reqPartCategory,可能是this，也可能是this下的子PartCategory
        return query(reqPartCategory, constraintReq);
    }



"sd:sum.Quantity ==2 where Speed=5400"


TODO：idea这个世界，是把很多事情揉在做，导致复杂？  把本身一体的事情 隔离做？  ---正反 有点意思  why?

     ENUM(0, "EnumType"),//Y=0, XX=0
	 RANGE(2, "Range")
	 ORGINAL(1,"ORGINAL");

	 value 

定义
enum BaseType {
    INT(0, "Integer"),
    STRING(1, "String"),
    BOOL(2, "Boolean"),
    FLOAT(3, "Float"),
    DOUBLE(4, "Double"); 
}

已经定义了
对类DynamicAttributeType更新如下：
修改前：
public enum DynamicAttributeType {
    Integer(0, "Integer"),
    String(1, "String"),
    Boolean(2, "Boolean"),
    Float(3, "Float"),
    Double(4, "Double");
	....
修改后：
public enum DynamicAttributeType {//YXX --XX是baseType,Y是CompositeType
    E_INT(0, "ENUM Integer"),
    E_STRING(1, "ENUM String"),
    E_BOOL(2, "ENUM Boolean"),
    E_FLOAT(3, "ENUM  Float"),
    E_DOUBLE(4, "ENUM Double"); 
	...
    B_INT(100, "base Integer"),
    B_STRING(101, "base String"),
    B_BOOL(102, "base Boolean"),
    B_FLOAT(103, "base  Float"),
    B_DOUBLE(104, "base Double");
	...
    public static int BASE_VALUE = 100;
	
	CompositeType getCompositeType() {
		 return CompositeType.fromValue(this.value / BASE_VALUE);
	}
	BaseType getBaseType() {
		 return BaseType.fromValue(this.value % BASE_VALUE);
	}

	enum BaseType {
		INT(0, "Integer"),
		STRING(1, "String"),
		BOOL(2, "Boolean"),
		FLOAT(3, "Float"),
		DOUBLE(4, "Double"); 
	...
	}
	public enum CompositeType { 
		ENUM(0, "EnumType"),
		BASE(1, "Base"),
		RANGE(2, "Range");
	....
	}
	.....
}
 
设计9：
1、类DAttrAnno* 增加 
    DynamicAttributeType dynAttrType() default DynamicAttributeType.E_INT;
2、对ModuleGenneratorByAnno的方法 processDynamicAttributeAnnotations等相应调整
 



1、把DynamicAttributeType的BaseType提取出来为一个独立的类
2、把DynamicAttributeType的CompositeTypeType提取出来为一个独立的类，并改名为ExtendedType
 

设计10：
1、在类Part增加方法
 queryDynAttrSchemas(String code) -> DynamicAttribute {....}

2、对类PartCategory做下列增加
AggregationType
2.1 新增函数sumInstanceDynAttrValue:
 String  sumInstanceDynAttrValue4Int(Part part,List<InstanceDynAttrValueItem> instValues,String attrCode) {
	int sumAttrValue = 0;
   
    for (InstanceDynAttrValueItem instValue : instValues) {
                    String attrValue = instValue.getInstAttr(attrCode);
                    if (attrValue == null) {
                        throw new IllegalArgumentException(
                                "Attribute '" + attrCode + "' value is null for part '"
                                        + part.getCode() + "'");
                    }
                    try {
                        sumAttrValue += Integer.parseInt(attrValue);
                    } catch (NumberFormatException e) {
                        throw new IllegalArgumentException(
                                "Invalid number format for attribute '" + attrCode
                                        + "' with value '" + attrValue + "' in part '" + part.getCode() + "'",
                                e);
                    }
   }
   return String.valueOf(sumAttrValue);
}
2.2 参考 sumInstanceDynAttrValue4Int实现
 
sumInstanceDynAttrValue4Float  -->和Int一致
sumInstanceDynAttrValue4Double  -->和Int一致 

                
2.3 修改函数 
修改前：
  PartCategory query(PartCategory category, PartConstraintReq constraintReq) {
	  ....
	  int sumAttrValue = 0;
	  ...
	  Part cPart = part.clone();
      cPart.setAttr(attrResult.getFirst().getCode(), String.valueOf(sumAttrValue));
      filterParts.add(cPart);
	  ....

 }

修改后： 
  PartCategory query(PartCategory category, PartConstraintReq constraintReq) {
	  ....
	  Part cPart = part.clone();
	  String  sumAttrValue = 0;
	  List<DynamicAttribute> instAttrs = category.queryDynAttrSchemas4Inst();
	  for(DynamicAttribute instAttr: instAttrs) {
		switch(instAttr.getDynAttrType().getBaseType) {
			case INT: 
				sumAttrValue = sumInstanceDynAttrValue4Int(...);
				cPart.setAttr(instAttr.getAttrCode(), sumAttrValue);
				break;
			case FLOAT: .... 
			case DOUBLE: .... 
			default:  ...;
		}
	  }
	  
	  ... 
      filterParts.add(cPart);
	  .... 
 }


设计1：

1、新增一个一个接口
interface IModule {
	Optional<Para> getPara(String code);
	Optional<Part> getPart(String code);
	List<Rule>  getRules();
}
2、Module实现这个IModule
3、PartCatagory实现这个IModule  
增加List<Rule> rules;
增加List<Para> paras;

4、CodeRuleAnno增加字段
String fatherCode;

5、ComputerOptiSolutionTest
rule1,rule2的fatherCode= "drive"  

6、对类ModuleGenneratorByAnno做下列更新 

createRulesFromMethods 逻辑改成，根据fatherCode添加到对应rules中，如果是null或"",则添加到Module中，否则添加到对应的PartCategory中




设计11：

    /**
     * 部件分类代码
     */
    private String partCatagoryCode;



1、在com.jmix.executor.omodel增加下列类

@Data
public class InferPartCategoryReq { 
     
    /**
     * 前置参数实例列表
     */
    private List<ParaInst> preParaInsts;

    /**
     * 前置部件实例列表
     */
    private List<PartInst> prePartInsts;

    /**
     * 部件约束请求列表
     */
    private List<PartConstraintReq> partConstraintReqs;

    /**
     * 是否枚举所有解, false，仅返回一个可行解，true，则返回所有的可选解
     */
    private boolean enumerateAllSolution = false;

    /**
     * 部件分类代码
     */
    private String partCatagoryCode;

	private boolean isEnumerateAllSolution;
}


2、ModuleConstraintExecutorImpl做下列改变
2.1 
修改前：
Result<List<ModuleInst>> inferParas(InferParasReq req) {
	...
	module.init();

}
修改后：
 
Result<List<ModuleInst>> inferParas(InferParasReq req) {
	...
	module.init();
	PartCategoryConstraintExecutorImpl partCatagoryExcutor= new ...
	cReq = toInferPartCategoryReq(req);
	partCatagoryExcutor.processPartCategory(....);
}
   
2.2 增加函数 toInferPartCategoryReq(InferParasReq req) -->InferPartCategoryReq  {...}
 


3、新建一个类PartCategoryConstraintExecutorImpl
class PartCategoryConstraintExecutorImpl {

Result<List<ModuleInst>> processPartCategory(InferPartCategoryReq partCatagoryReq, Module module,boolean isAttachRelax,List<RelaxVar> confictedRelaxs) {
	ConstraintAlgImpl alg = createConstraintAlg(module.id,module.code);
    filteredCategory = module.findPartCategory(partCatagoryReq.partCatagoryCode);
	for (PartConstraintReq partConstraintReq : partCatagoryReq.partConstraintReqs) {
            // 查询满足条件的PartCategory
            filteredCategory = partCategory.query(partConstraintReq); 
	}
 	CpModel model = new CpModel();
	alg.initModel(model, module, isAttachRelax=false, confictedRelaxs);//confictedRelaxs
    initModelByReq(filteredCategory,req,alg); 
        // 添加松弛目标函数, 方便调试
    alg.addRelaxObjectFunction();


    if (config.isLogModelProto()) {
            // 将module的CpModelProto信息输出到文件config.logFilePath/module.proto.txt
            model.exportToFile(config.getLogFilePath() + File.separator + module.getCode() + ".proto.txt");
    }
	CpSolver solver = new CpSolver();
        solver.getParameters().setEnumerateAllSolutions(req.isEnumerateAllSolution());
        solver.getParameters().setNumSearchWorkers(1); // 单线程搜索，防止有重复解
        log.info("solver parameters:\n" + solver.getParameters().toString());
        // 可按需设置更多参数
        ModuleInstSolutionCallBack cb = new ModuleInstSolutionCallBack(module, alg.getVars(), alg.getOtherVarMap());
        CpSolverStatus status = solver.solve(model, cb);
  // 如果模型无效，调用ValidateCpModel获取详细错误信息
            if (status == CpSolverStatus.MODEL_INVALID) {
                // 重新获取模型进行验证
                ConstraintAlgImpl alg = initConstraintModel(module, req, false, new ArrayList<>());
                CpModel model = alg.getModel().getCpModel();
                String validationError = model.validate();
                log.error("Model validation failed: {}", validationError);
                return Result.failed("Model validation failed: " + validationError);
            }

            if (status != CpSolverStatus.OPTIMAL && status != CpSolverStatus.FEASIBLE
                    && status != CpSolverStatus.INFEASIBLE) {
                return Result.failed("solver status: " + status);
            }
			if (status == CpSolverStatus.INFEASIBLE ) {
				 return Result.sucess("solver status: " + status);
			}
}
 private void initModelByReq(PartCategory filteredCategory，InferPartCategoryReq req, ConstraintAlgImpl alg) {

        if (req.getPreParaInsts() != null) {
            for (ParaInst paraInst : req.getPreParaInsts()) {
                alg.addParaEquality(paraInst.getCode(), paraInst.getValue());
            }
        }
        if (req.getPrePartInsts() != null) {
            for (PartInst partInst : req.getPrePartInsts()) {
                alg.addPartEquality(partInst.getCode(), partInst.getQuantity());
            }
        }


            // 获取所有叶子部件
            List<Part> filterParts = filteredCategory.getAllLeafParts();
            log.info("filterParts size: {} after filter req.whereCondition: {}", filterParts.size(),
                    partConstraintReq.getAttrWhereCondition());
            List<Part> unFilterParts = PartCategory.calcUnFilterLeafParts(partCategory, filterParts);

            // 解析属性
            Pair<DynamicAttribute, String> result = partCategory.parseAttribute(partConstraintReq.getAttrCode(),
                    partCategory.getDynAttrSchemas());

            // 根据result.second构建约束表达式
            if (AttrFunConstant.FUN_PREFIX_SUM.equals(result.getSecond())) {
                alg.sumFunConstraint(filterParts, result.getFirst().getCode(), partConstraintReq.getAttrComparator(),
                        Integer.parseInt(partConstraintReq.getAttrValue()));
                alg.setPartUnSelected(unFilterParts);
            }
 }


设计11：

1、参考AlgCPModel，对com.google.ortools.sat.TrackedLinearExpr包装一个AlgTrackedLinearExpr记录类似sumFormulaBase的exprStr 
2、sum4Parts中的sumFormulaBase放到exprStr中
3、参考 sumFunConstraint中，在model中addLessOrEqual打印完整的公式
修改前：
               sumTerms.add(TrackedLinearExpr.term(partVar.getQty(), attrValue));
                sumTermStrings.add(partVar.getBase().getShortCode() + ".Q*" + attrValue);

修改后：
               sumTerms.add(TrackedLinearExpr.term(partVar.getQty(), attrValue));
                sumTerms.addStr(partVar.getBase().getShortCode() + ".Q*" + attrValue);
4、
   model.minimize(TrackedLinearExpr.sum(weightedTerms));



请按下列设计进行开发，并完成ComputerOptiSolutionTest的测试

0、类InferPartCategoryReq增加
int maxSolutionNum; //最大solution的个数

 

1、增加下列类
class PriorityConstraint {
    String ruleCode;
	TrackedLinearExpr  expr;
	  PriorityStrategy  strategy 
}

enum PriorityType {
  SELECT,//选择性，和part.isSelected相关，例如：优先匹配高速率容量
  SUMARIZE,//汇总性，和part.qty相关，例如：总容量越大于浩 
}


enum PriorityStrategy {
  MAX,//最大
  MIN,//最小 
}

2、对类ConstraintAlgImpl做下列增强：
class ConstraintAlgImpl {
....
....
}

修改后：
class ConstraintAlgImpl {
....
Map<String,PriorityConstraint> priorityRuleMap;


	void buildRuleMethods() {
		....
		调用buildPriorityConstraint 放到priorityRuleMap中<attrCode,xxx>
		....
	}
	void buildPriorityConstraint(Rule rule) {
		PriorityConstraint pConstraint = ....;
		pConstraint.expr = sum4Selected(rule.attrCode); 
		...
	}
....
}



4、参考CompatiableRuleSchema，增加类 PriorityRuleSchema  

@Data
@EqualsAndHashCode(callSuper = true)
public class PriorityRuleSchema extends RuleSchema {
 
     PriorityType type;
	 
	 PriorityStrategy  strategy 
	
	 String attrCode; 
}

5、参考CodeRuleAnno，新增注解PriorityRuleAnno

 @interface CodeRuleAnno {
    /**
     * 规则代码
     */
    String code() default "";

    /**
     * 自然语言描述
     */
    String normalNaturalCode() default "";

    /**
     * 父部件编码，如果为null或空字符串，则规则添加到Module中；否则添加到对应的PartCategory中
     */
    String fatherCode() default "";

	String attrCode;
	
     PriorityType type;
	 
	 PriorityStrategy  strategy 
}

6、类 ComputerOptiSolutionConstraint中rule2改为：

//优先性策略：
@PriorityRuleAnno(fatherCode="drivie",normalNaturalCode="固态硬盘优先匹配高速率容量，用机械硬盘增配低速率容量"
	rawCode="选择的部件capacityWeight总和越大越好",
   attr="capacityWeight", strategy="MAX",type="SELECT")

private void rule2() {

}

7、对PartCategoryConstraintExecutorImpl做下列增强
参考@doc/opti.txt 代码，实现对 PriorityRule分级求解
修改前： 
....processPartCategory() {
	....
	...
	return solveConstraintModel(alg, filteredCategory, isAttachRelax, confictedRelaxs, partCatagoryReq, module);
}
  

修改后： 
....processPartCategory() {
	....
	...
	// 步骤1：先每个优化解最优解 
	对rule中每条PriorityRule (attr1)
		调用solveConstraintModel方法完成目标解求解 attr1Obj目标解
    
	// 步骤2：在保持高优先级目标接近最优的前提下，寻找多个解(attr1Obj 的30%上浮和下降）
		调用solveConstraintModel 
	//对解按优先级排序
	  
}
 
设计112：





类PriorityConstraint改成如下：

public class PriorityConstraint {

   /**
     * 规则代码
     */
    private String ruleCode;

    /**
     * 线性表达式
     */
     private TrackedLinearExpr expr;//约束求解器里的表达式


	private String exprStr;//如： sd1.S*110  +sd2.S*120 + md1.S*13

	private String exprTemplate;//如：%d*110 + %d*120 + %d*13,用于计算值(（是否一致)

	private String exprTemplateStr;//如： sd1.S_%d*110  +sd2.S_%d*120 + md1.S_%d*13  用于暂时

	private List<PartTerm> exprVariables = new ArrayList();

	public static String calcToString(PriorityConstraint pc, List<PartTerm> exprVariables) {
		根据exprVariables中termValue的构筑 objectValues
		calcExprStr = String.format(exprTemplate,objectValues); 
		调用公式计算机器计算calcStr的值calcResult，基于  Apache Commons JEXL 3.4.0 实现
		printStr = String.format(exprTemplateStr,objectValues); 
		打印 printStr = calcResult
	}



	class  PartTerm
	{
		- int  index 
		- String partCode
		-int termValue=默认是空//, 可能是“.S"的值，也可能是".Q"的值  
	}

}


设计112：

1、对类ModuleInst做下列增强
  添加字段
  List<PriorityAttrValue> priorityAttrValues; //每个约束的值
  int prioritySortNo = 0;//排序号
  double priorityOverallValue;//最后综合的值



2、在ModuleInst所在的package增加下列类
@Data
class PriorityAttrValue {

    /**
     * 属性代码
     */
    private String attrCode;

    /**
     * 最优值
     */
    private double optimalValue;
}


3、类PriorityAttrObjValue改成PriorityAttrValueImpl
定义如下
@Data
public class PriorityAttrValueImpl extends PriorityAttrValue { 

	PriorityConstraint pConstraint;
	
	String getAttrCode() {...}
	... getPriorityType() {...}
	... getPriorityStrategy {...}
}


4、对ConstraintAlgImpl做下列增强

queryPriorityConstraint(String attrCode) {
...
}


5、对类PartCategoryConstraintExecutorImpl做下列增强
5.1 方法solveWithPriorityConstraints
修改前：
  ...solveWithPriorityConstraints(...) {
	....
	         // 重新构建目标函数表达式（使用新的alg实例）
            TrackedLinearExpr objectiveExpr;
            if (priorityType == PriorityType.SELECT) {
                objectiveExpr = optAlg.sum4Selected(attrCode);
            } else {
                objectiveExpr = optAlg.sum4Quantity(attrCode);
            }
	....
	....
	
  }

修改后：
  ...solveWithPriorityConstraints(...) {
	....
	   PriorityConstraint pConstraint= optAlg.queryPriorityConstraint(attrCode);
		objectiveExpr = pConstraint.expr;

	....
		objValue.pConstraint = pConstraint
	....
	
  }

5.2 sortSolutionsByPriority
  根据solutions中每个ModuleInst.priorityOverallValue进行排序，然后 
  对ModuleInst.prioritySortNo 进行赋值



6、对类ModuleInstSolutionCallBack做下列增强
6.1 增加成员变量
List<PriorityAttrValueImpl> priorityAttrValues;
6.2 现有方法onSolutionCallback(
修改前：
 public void onSolutionCallback() {
	....
        // 处理其他变量
        processOtherVariables(moduleInst);

        allSolutions.add(moduleInst);
}
修改后：
 public void onSolutionCallback() {
	....
        // 处理其他变量
        processOtherVariables(moduleInst);

        allSolutions.add(moduleInst);
}

6.3 增加方法processPriorityValues()
void  processPriorityValues(moduleInst) {
	List<PriorityAttrValue> priorityAttrValues = ...
	for(priorityAttrValue:priorityAttrValues) {
		List<Integer> exprVariablesValues = ...
		for(exprVariable: priorityAttrValue.pConstraint.exprVariables) {
			PartInst pInst = moduleInst.queryPart(exprVariable.partCode);
			如果pInst== null,则exprVariablesValues.add(0);
			否则：
				如果priorityAttrValue.getPriorityType() == PriorityType.SELECT 则  exprVariablesValues.add(1);
				否则  exprVariablesValues.add(pInst.getQty()); 
			
		}
		double optimalValue = PriorityConstraint.calcToString(priorityAttrValue.pConstraint，exprVariablesValues);
		priorityAttrValues.add(...optimalValue);
	}
	moduleInst.priorityAttrValues = priorityAttrValues;
 
	moduleInst。priorityOverallValue  = 对priorityAttrValues.optimalValue 取平均值
}
 
 

设计121：

1、定义一个数据类
class ParConstraint{
	String sumAttrCode
    String comparator
	int leftValue
}

2、类PartCategoryConstraintExecutorImpl增强如下；
2.1 方法 processPartCategory增强 
修改前：
 ... processPartCategory(...) {
	...
	filteredCategory = filteredCategory.query(partConstraintReq);

	...
 }

修改后：
... processPartCategory(...) {
	List<ParConstraint> partConstraintFromReqs = ....
	...
	filteredCategory = filteredCategory.query(partConstraintReq);
	resolveAddPartConstraint(...)
	...
 }
2.2 增加函数
void resolveAddPartConstraint(filteredCategory,partConstraintReq，partConstraintFromReqs)

	            // 解析属性
              Pair<DynamicAttribute, String> result = filteredCategory.parseAttribute(
                    partConstraintReq.getAttrCode(),
                    filteredCategory.getDynAttrSchemas());
			  if (!AttrFunConstant.FUN_PREFIX_SUM.equals(result.getSecond())) {
					return;
			  
			  根据partConstraintReq构造ParConstraint ->fromReq
               partConstraintFromReqs.add(fromReq)
}
2.3 函数solveWithPriorityConstraints 增加入参 partConstraintFromReqs
2.4 函数 initModelByPriorityConstraints改成

initModelByPriorityConstraints(PartCategory filteredCategory,
            List<ParConstraint> partConstraintFromReqs, ConstraintAlgImpl alg) {
	  List<Part> filterParts = filteredCategory.getAllLeafParts();
	  遍历partConstraintFromReqs，调用alg.sumFunConstraint
}

3.ConstraintAlgImpl增强如下
3.1 方法sumFunConstraint
3.1.1 
sumFunConstraint(List<Part> sumParts, String sumAttrCode, String comparator, int leftValue) 
改成：
sumFunConstraint(List<Part> sumParts, ParConstraint partConstraint)
3.1.2  内部实现尽量使用buildPriorityConstraintExpressions

 

设计122：按下列设计写代码，并调通ComputerOptiSolutionTest
1、ConstraintAlgImpl类做下列增强
1.1  所有sum4Selected、sum4Quantity、sum4Parts方法都增加 filtedConditionStr
1.2 buildPriorityConstraintExpressions增加入参：List<Part> atomicParts
1.3 方法 sum4Parts
修改前：
   ... sum4Parts(...) {
		...
		buildPriorityConstraintExpressions(tempConstraint, cofAttrCode, varName, varGetter);
		....
	
	}
修改后：
   ... sum4Parts(...) {
		...
		List<Part> atomicParts = module.getAtomicParts();
		调用 FilterExpressionExecutor.doSelect(atomicParts,filtedConditionStr);
		调用PartUtils.toShortString打印
		buildPriorityConstraintExpressions(tempConstraint, cofAttrCode, varName, varGetter);
		.... 
	}

4、类ComputerOptiSolutionTest 
4.1 方法rule1
修改前：
void rule1() {
	...  sum4Selected();
	...
	... sum4Quantity();
	...

}
修改后：
void rule1() {
	...  sum4Selected("fatherCode=sd");
	...
	... sum4Quantity("fatherCode=sd");
	...

}
 

TODO: 最大解个数设置错了，需要优化？

TODO:产品有多个Priv值怎么办？ ***


 @src/main/java/com/jmix/executor/impl/SolutionUtils.java:55-56  打印 "1.4 Priority Attribute shortName:  printPriorityAttribute(...)   , 直接 @src/main/java/com/jmix/executor/omodel/ModuleInst.java:177-183 


OptiStrategyReq optiStrategy;

2、
先过滤：
每个优先都一个单独求解的过程？ ---按当前约束？ 算一次
currentMax  

objFun > * currentMax * 30%


都要变成最小值问题：
  minValue_D1
  max 
for(OptiStrategyAttrReq dimAttrReq: attrReqs) {
	//这个维度先求
    

}



使用场景？ 使用场景？ 怎么做？ 

总总量相关的 qty*dimeAtt，如果仅是选择相关，则用selected即可？
@maximize

InferPartCategoryReq

 

1、目录价最小
{"attrReqs":[{"attrCode":"ListPrice", "strategy":"MIN"}]
2、先目录加最小，货期最小  
{"attrReqs":[{"attrCode":"ListPrice", "strategy":"MIN"},{"attrCode":"Delivery", "strategy":"MAX"}]
3、货期最小，同时利润最大
{"attrReqs":[{"attrCode":"Delivery", "strategy":"MIN"},{"attrCode":"Profit", "strategy":"MAX"}]

class PriorityAttrConstant {
String LIST_PRICE= "ListPrice";  
String DELIVERY = "Delivery";  
String PROFIT = "Profit";  //double  
}

class PriorityAttrStrategyReq {
	String attrCode;
	PriorityStrategy  strategy;
}
class PriorityStrategy {
  List<PriorityAttrStrategyReq> attrReqs;
}


多维目标求解：

多因子目标求解




都变成最小问题



最优解？
---


因子：


最大解的个数


多个目标：
最大问题，变成最小问题？ 






 

2026-1-11：
TODO： 133.0  设置max，没有达到最大数量怎么办？

TODO：先忙着物理上的约束的多解，在满足的  其它维度的多解？  现满足基本要求？  再是其它的解？ 
---成本，Profit， 简单算法？ --- 
---多维度求解？   
---两个物理上约束的最优解，然后是获取、成本等，最优解？ ***
一个partCategoy有多个吗？ 

---多个在一起，怎么计算是？  ---放在一起？ --- （要放在一起计算） 
---


TODO：画一个约束模型等？ --**  Part selected  qty 

TODO: moduleInst 和PartCategoryInst  总体的调度框架

TODO: 多解调优，如果多解，目标函数近视函数，如果有问题，怎么结合releax, 目标的百分比，分成几档，结合业务场景两讲
TODO：多解不同界面适合的场景，在配置领域，让ai给出方案？ ---
TODO：有多解后，怎么结合大模型给出的解释？ 去试一下？ ---**

TODO：增加多priority的处理 --**
Priority

TODO: 约束冲突的问题
            if (status == CpSolverStatus.INFEASIBLE && config.isDebugByRelaxVar()) {
TODO:       // 一条partConstraintReq会被拆分两条规则，一条是过滤规则（根据where条件过滤），一条是约束规则（根据attrCode,
                // attrComparator, attrValue构建约束表达式）  

 这么拆分可能也问题，约束对某些部件来说的，但是对同一个？PartCategory? 要论证
   


TODO: 类有点乱zhaozhao

2.2 


TODO:后续要改成  Module改成ModuleImpl



先跑通
CPU和Drivier两种场景？ --单个场景
先过滤，在约束？求解？ -----先大过滤？
先满足影响约束，再满足类似什么最优？ 后续设置属性？ 约束可能要硬性和软性的类似？ 
----类似业务软约束？ 硬件 约束--- A和B不兼容等
----确定性和不确定性，什么优先？ ---看看分那几种？
---销售策略？ ---优先？  
-----约束也有优先级? ****  

----实在是搞不定的，这个Module，开发出来，自己来定制，后续类似FDE工程师，来定制？ 定制一个最优版本了？ 

----多维求解从理论上是一个NP问题？ 
-----业务建模上，怎么编程可执行的问题？  如果让性能更快？——————



多维求解的过程验证一下？
TODO： Rule -->Logic 
TODO：多PC的写吧。景，CPU和 DIRVE直接有逻辑？ 


TODO: idea 大模型时代 模糊化语言？不讲细节？ 重点将过程？ ---**
1.2 修改函数：addPartConstraintReqs
TODO：  方案1： 加载是动态，只加载需要的变量？ ---**  长期   设计到partVar初始化等过程？ ***
TODO:多个条件在一起？ **

TODO； Partcatagory也是一个Module，要接口化一下？ 暂时不这样做？
  PartCategory也有Parameter

TODO：支持设置shortCode，


TODO：把访问大模型的接口换一下？**

TODO： 是bug
            Map<String, String> extAttrs = parseAttributes(partAnno.extAttrs(), new ArrayList<>());

TODO: 所有设置系统函数的ConstraintAlglmpl的地方，都要打日志，最后单独抽取出来，方便查看？***

public enum DynamicAttributeType {  DAttrAnno11 及解析
    Integer(0, "Integer"),  --类型明确一下


TODO： PartInst的怎么处理？  是否要把输入都独立出来得了？
    private Map<String, String> selectAttrValue;


TODO: 引擎接口要抽取出来？
    public void helloSample() throws IOException {



TODO comparator常量 
 TODO： diver根据分类类，可能要分， Part还是用模型建立起来比较好？感觉？*** （数据存储上不区分，但是在类上要区分，行为不同）

	//TODO：单个纯粹过滤，待定？ 例如版本等事情？ 
isHidden要去掉？  TODO  

先过滤  

TODO：module整个数据就是按本体化组织，按PartCategory组织---*



TODO: 如果没有partCategory 
TODO: filterConstrants 是 and 关系
TODO: 先做一个PartCategory,然后再考虑其它  
   考虑到父分类，一个分类是否相同条件，包括， 单选、多选？ ---》这些封装很重要？***
 
 TODO： relax的权重，和这里的权重的差别，怎么融合？

TODO: capacityWeigh(Integer) --  使用枚举值

5、不能使用完全不包含用户需要的速率的固态硬盘做配置  --先过滤和执行能满足，那如果没有这个要求，怎么办？

TODO： 基于一个PartCategory怎么做扩展，自己写规则***  

TODO：名字要改：
@Data
public class InferParasReq {







4、类Module增加
	@jsonignore
    private List<Part> backParts = new ArrayList<>();
	
	@jsonignore
	public List<Part>  doSelect(String rootCategoryCode,List<PartConstraintReq> filterConstrants) {			
			根据rootCategoryCode，从this.parts 查到 rootPartCategory 
		   backUpParas(); 
		   PartCategory filtedPartCategory = doSelect(filterConstrants,rootPartCategory);
		   根据filtedPartCategory对this.parts进行重新构造
		   return this.parts;
	}
	@jsonignore
	public PartCategory  doSelect(List<PartConstraintReq> filterConstrants, PartCategory rootPartCategory ) {
			 for(PartConstraintReq cReq) { 
				rootCategory = rootCategory.query(cReq);
			 } 
	}

	
		@jsonignore
	public void  backUpParts( ) {
			this.backParts  =  this.parts;
		 
			this.paras = new ArrayList(); 
	}
		@jsonignore
	public void  recoverParas( ) {
				this.paras  =  this.backParas;
			    this.backParas = new ArrayList();	
	}



1、优先？ 



要求5400速率的固态硬盘2块，要求5400速率的硬盘容量>=5T， 


MechDrive
请参考下列模型来基于ortools-java来构建约束模型。

样例数据： 

--电脑  --产品
---硬盘  --PartCategory1
------固态硬盘			数量			接口带宽	 	支持转速容量分组  --PartCategory11
-----------固态硬盘1					8GB/S	    [{”实例分组“：1，“转速”：“5400”，“容量”：“2T”}，{”实例分组“：2，“转速”：“7200/5400”，“容量”：“1T”}]
-----------固态硬盘2					8GB/S	    [{”实例分组“：1，“转速”：“7200/5400”，“容量”：“4T”}]
-----------固态硬盘3					8GB/S	    [{”实例分组“：1，“转速”：“9000”，“容量”：“4T”}]
------机械硬盘			数量			支持转速容量分组 --PartCategory12
-----------机械硬盘1					 { “转速”：“5400”，“容量”：“2T”} 
-----------机械硬盘2					 { “转速”：“7200”，“容量”：“2T”} 
-----------机械硬盘3					 [ “转速”：“9000”，“容量”：“2T”} 

PartCategory1的详细定义
部件编码	扩展属性	扩展属性类型	扩展属性值				实例分组
硬盘		转速		规格			5400|7200/5400|9000	1
硬盘		容量		规格									1
硬盘		接口带宽	规格			8GB/S					-1


关键配置逻辑：
1、固态硬盘必须配置同一种，并且最多配置2块
2、优先使用固态硬盘，固态硬盘满配后还不满足要求的用机械硬盘补充
3、用户提了固态硬盘数量的，使用固态硬盘数量，否则需要计算固态硬盘数量
4、固态硬盘优先匹配高速率容量，用机械硬盘增配低速率容量
5、不能使用完全不包含用户需要的速率的固态硬盘做配置



输入：
要求5400速率的固态硬盘2块，要求5400速率的硬盘容量>=5T， 

 
##数据模型

// 可编程接口
public interface Programmable {
/**

获取编码
@return 编码
*/
String getCode();
}
// 可扩展对象
public class Extensible {
/**
* 扩展属性schema
*/
private String extSchema;

/**
 * 扩展属性，方便扩展
 */
private Map<String, String> extAttrs = new HashMap<>();
}

// 可编程对象
public class ProgrammableObject<T> extends Extensible implements Programmable {
/**
* 对象编码
*/
private String code;

/**
 * 父对象编码
 */
private String fatherCode;

/**
 * 默认值
 */
private T defaultValue;
 
/**
 * 排序号
 */
private Integer sortNo;
}

// 动态属性
public DynamicAttribute extends ProgrammableObject<String> {
private String code;
private String name = ""; // 名称
// 动态属性可选值列表
private List<DynamicAttributerOption> options = new ArrayList<>();
// 动态属性类型
private DynamicAttributeType dynAttrType = DynamicAttributeType.Integer;
// 动态属性的值，拥有计算实例化的值，存在算法
private String value;
// 动态属性的Logic
private List<Logic> valueLogics;
}

// 动态属性可选值
public class DynamicAttributerOption extends ProgrammableObject<String> {
// 选项编码ID
private int codeId;
}

// Para调整为：
public class Para extends DynamicAttribute{
private ParaType paraType = ParaType.ATOMIC;
// 默认值
private String defaultValue;
// 引用的规格Code
private String refSpecCode;
}

// 参数类型调整为：
public enum ParaType{
ATOMIC(0, "AtomicPara"),
GROUP(10, "Group");//类似中间件层
}

// Part调整为
public class Part extends ProgrammableObject<Integer> {
/**
* 部件类型
*/
private PartType partType = PartType.ATOMIC;

/**
 * 原子Part的目录价
 */
private Long price = 0L;

/**
 * 原子Part的最大数量
 */
private Integer maxQuantity = MAX_QUANTITY;

/**
 * 原子Part的规格属性(动态属性值）
 */
private Map<String, String> dynAttr = new HashMap<>();

/**
 * Part分类的规格描述(动态属性定义,partCategoryAttrValues)
 */
private List<DynamicAttribute> dynAttrSchemas= new ArrayList<>();

/**
 * Part分类的schema (partCategoryAttrSchema?)
 */
private String dynAttrSchema;
 
}

// Part类型
public enum PartType {
ATOMIC(0, "AtomicPart"),
CATEGORY(2, "PartCategory"),
BUNDLE(3, "Bundle"),
GROUP(10, "Group");//类似中间件层
}

TODO:
固态硬盘数量-->硬盘数量private List<Logic> logics;
}

 
1、Para.java
public class Para extends DynamicAttribute { 
    private ParaType paraType = ParaType.ATOMIC;
....

}

改成：
public class Para extends DynamicAttribute { 
    private ParaType paraType = ParaType.STRING;
....

}


2、ParaType调整为：

+ public enum ParaType { //YXX --XX是subType,mainType
+     ENUM(0, "EnumType"),//Y=0, XX=0
+     BOOLEAN(1, "Boolean"),//Y=0, XX=0
+     INTEGER(2, "Integer"),//Y=0, XX=0
+     FLOAT(3, "Float"),//Y=0, XX=0
+     DOUBLE(4, "Double"),//Y=0, XX=0
+     STRING(5, "String"),//Y=0, XX=0
+     RANGE(6, "Range"),//Y=0, XX=0
+     DATE(7, "Date"),//Y=0, XX=0
+     MULTI_ENUM(8, "MultiEnum"),//Y=0, XX=0
+     GROUP(100, "Group");//Y=1, XX=00(STRING)
+     
+     private final int value;
+     private final String name;
	
	  public static int BASE_TYPE = 100; 
	  int getSubType() {
			return this.value % BASE_TYPE;
	  }
	  ParaMainType getMainType() {
		   return this.value / BASE_TYPE;
	  }
	public enum ParaMainType{
		ATOMIC(0, "AtomicPara"), //原子para,默认是原子Para 
		GROUP(1, "Group");//类似中间件层
	} 
}


设计x11:先过滤再计算  过滤采用的 筛选法，还是筛选法？ ---

1、扣除法     
deductPartCodes


1、类FilterExpressionExecutor
增加一个函数
    /**
     * 根据扣除表达式获得扣除后的对象列表，例如:
     * partCode speed capacity
     * part1 5400 1T
     * part2 7200 2T
     * part3 9000 4T
     * deductExpr = speed 5400,则返回结果 part2，part3
     * 
     * @param objects    要扣除对象列表-全
     * @param deductExpr 扣除表达式
     * @param <T>        对象类型，必须继承自Extensible
     * @return 扣除后的对象列表
     */
    public static <T extends Extensible> List<T> doDeduct(List<T> objects, String deductExpr) {
		Optional<FilterCondition> deductCondition = parseFilterExpression(deductExpr);
		将deductCondition转化过滤表达式 filterCondition = !deductCondition ---写一个函数,并补充相应的测试用例
          --根据operator来， 如果deductCondition.opereator = "=" ,则 filterCondition.opereator ="!=" 
          --根据operator来， 如果deductCondition.opereator = "=" ,则 filterCondition.opereator ="!=" 
		  ....
	}

2、类增加OP_NOT_LIKE

 public static final String OP_NOT_LIKE = "not like";


设计xxx1

 

1、对类ParConstraint做系列增强：
增加属性 
  PartCategory filteredCategory

2、对类PartCategoryConstraintExecutorImpl做下列增强
2.1 方法initModelByPriorityConstraints
改前:
    private void initModelByPriorityConstraints(PartCategory filteredCategory,
            List<ParConstraint> partConstraintFromReqs, ConstraintAlgImpl alg) { 
	...
	 alg.sumFunConstraint(filterParts, partConstraint);
	...
}
改后：
    private void initModelByPriorityConstraints( 
            List<ParConstraint> partConstraintFromReqs, ConstraintAlgImpl alg) { 
	...
	 alg.sumFunConstraint(partConstraint.getFilteredCategory().getAtomicParts(), partConstraint);
	...
}

2.2 增加函数
PartCategory buildMergedFilteredCategory(PartCategory originalCategory,List<ParConstraint> partConstraintFromReqs) {
	//仅支持两层结果，不构建结构
	//扣除法的原则
//实现案例
 //--drive PC 
 //----sd  PC 
 //-------sd1 part 
 //-------sd2 part 
 //-------sd3 part 
 //----md  PC 
 //-------md1 part 
 //-------md2 part 
 //-------md3 part 

//含有父，则求并集
//input1: req1:{sd->sd1,sd2} req2:{drive-> md1} --> mReq={sd1,sd2,md1} 
//input1: req1:{sd->sd1,sd2} req2:{drive-> sd1} --> mReq={sd1,sd2} 
---不还原其层次结构

//不含有父，补充没有在里面的子分类的部件
//input1: req1:{sd->sd1,sd2} req2:{md->md1} --> mReq={sd1,sd2,md1}  // 各个孩子都有
//input2: req1:{sd->sd1,sd2} req2:{sd->sd1} --> mReq={sd1,sd2,md1,md2,md3} //部分 各个孩子都有 ***求交集？
   

	//<partCode,Part>
	Map<String,Part> meregedAtomicParts = ...
	Set<String> allPartCatagoryCodes = ...
	for(partConstraintFromReq: partConstraintFromReqs) { 
		allPartCatagoryCodes.add(partConstraintFromReq.orgReq.getPartCatagoryCode())
		meregedAtomicParts.putAll(partConstraint.getFilteredCategory().getAtomicParts()); 
	}
	//判断父是否包含
	if(!allPartCatagoryCodes.contains(originalCategory.getCode()) {
	{
		//要把没有包含的补充上
		for(subPartCategory,allPartCatagoryCodes.getpartCategoryMap().values)
		{
			if(!allPartCatagoryCodes.contains(subPartCategory.getCode()) {
				meregedAtomicParts.addAll(subPartCategory.getAtomicParts);
			}
		}
	} 
	resultCategory = originalCategory.clone();
	resultCategory.addParts(meregedAtomicParts);//不构建结构
	return resultCategory；
}


2.3 方法processPartCategory修复如下：
改前：
 ...processPartCategory(...)
 {
	...
	...
	solveWithPriorityConstraints(alg, filteredCategory, isAttachRelax, confictedRelaxs,
							partCatagoryReq, module, partConstraintFromReqs)
	...
  }
改后：
 ...processPartCategory(...)
 {
	...
	...
	mergedFilteredCategorg = buildMergedFilteredCategory(originalCategory, partConstraintFromReqs);
	solveWithPriorityConstraints(alg, mergedFilteredCategorg, isAttachRelax, confictedRelaxs,
							partCatagoryReq, module, partConstraintFromReqs)
	...
  }
仅在下列package创建代码，不能修改其它地方的代码
package com.jmix.temp2;


请按下列设计，实现优先类规则的求解 
要求使用java 的ortools-java 
##1、设计 
###1.1、核心类
class ProductSolver {
{
	//要求5400速率的硬盘容量>=5T-->strReq =" Capacity >=5 where Speed = 5400"
	ProductResult solve(String strReq) {
		将strReq转换为req：PartConstraintReq
		加载所有的parts:List<Part>
        调用FilterExpressionExecutor.doSelect(parts,req.attrWhereCondition) 得到 filterdParts;//如：md1，sd1 
        根据filterdParts构建约束变量 partVars:List<PartVar> 
		根据req和partVars来构造约束  //如：sd1.qty* 3 + md1.qty* 2 >=5
		根据proRule2/proRule1和partVars构建约束  
		进行求解,对解进行排序
	}
}		 

@Data
public class PartConstraintReq {

    /**
     * 属性代码
     */
    private String attrCode;

    /**
     * 属性比较符
     */
    private String attrComparator;

    /**
     * 属性值
     */
    private String attrValue;

    /**
     * 属性过滤条件
     */
    private String attrWhereCondition;

}


 
1、优先类规则：固态硬盘优先匹配高速率容量，用机械硬盘增配低速率容量 
解读1：如果指定了容量诉求，优先使用固态品牌满足，如果固态硬盘容量已足够，限制机械硬盘使用
解决2：如果没有指定容量诉求，尽量用固态硬盘满足，限制机械硬盘使用 
 

##2.产品的模型：
###2.1 模型定义
class Part {
	String code；
	BoolVar isSelected；//isSelect"（是否选中）
	IntVar qty;//配置数量“
    String speed;
	int capacity;
}
class PartVar   {
	Part part；
	String code；
	BoolVar isSelected；//isSelect"（是否选中）
	IntVar qty;//配置数量“
	getSpeed(){...}
	getCapacity(){...}
}
###2.2 产品优先类规则
proRule1:优先类规则：固态硬盘优先匹配高速率容量，用机械硬盘增配低速率容量 
解读1：如果指定了容量诉求，优先使用固态硬盘满足，如果固态硬盘容量已足够，限制机械硬盘使用
解决2：如果没有指定容量诉求，尽量用固态硬盘满足，限制机械硬盘使用

解释21：
---变量  sum.Capacity 

proRule2:固态硬盘必须配置同一种，并且最多配置2块

###2.3 产品样例数据        
        // ----sd PC  --固态硬盘 	 是否选中）  数量  speed  类型  容量 
        // -------sd1 			.isSelect .qty   5400   sd   3
        // -------sd2 			.isSelect .qty	 7200	sd   6
        // -------sd3 			.isSelect .qty   9000	sd   9
        // ----md    机械硬盘 	 是否选中）  数量  speed  类型 容量 	
        // -------md1 			.isSelect .qty	 5400   md  1
        // -------md2 			.isSelect .qty   7200	md  2
        // -------md3 			.isSelect .qty   9000	md  3
	                                                        	


###典型案例：

构建测试用例：
用例0： 测试点，解读1 
	输入：
	  strReq =  " Capacity >=6 where Speed = 5400"
	输出： 
	   
	  解1： sd1.qty=2    
	  解2： sd1.qty=1     md1.qty=3   //增配低速率容量 
	  ...
	     

用例1： 测试点，解读1 
	输入：
	  strReq =  " Capacity >=5 where Speed = 5400"
	输出： 
	 解1： sd1.qty=2  
	 解2： sd1.qty=1 md2.qty=1 
	 ...

用例2： 测试点，解读1 
	输入：
	  strReq =  " Capacity >=7 where Speed = 5400"
	输出：
	  sd1.qty=2   md1.qty=1 (固态硬盘容量不够，使用机械硬盘补充）
用例3： 测试点，解读2 
	输入：
	  strReq =  " Qty >=2 where Speed Speed = 5400"
	输出：
	  sd1.qty=2  

用例4： 测试点，解读2 
	输入：
	  strReq =  " Qty >=3 where Speed Speed = 5400"
	输出：
	  sd1.qty=2  md1.qty=1 (固态硬盘容量不够，使用机械硬盘补充）

用例5： 测试点，解读1 -固态硬盘优先匹配高速率容量
	输入：
	  strReq =  " Capacity >=5  "
	输出： 
	 解1： sd2.qty=1  
	 解2： sd1.qty=2  

2026-1-25：
  分2阶段求解
  建模过程统一化--抽取？  

  基于数量进行归一化？ 
  
  支持多个目标值求解

  动态可以调整？




对com.jmix.temp3做下列更新，不要修改其它地方的类
0、不要修改com.jmix.temp3.basic里的代码
1、类Part 改造如下：  

 // 部件类（数据模型）
@Data
public class Part {
    private String code;
    private boolean isSolidState; // 是否为固态硬盘
    private int speed;            // 转速
....
    private Map<String, Double> attrMap = new HashMap<>();       // 原始属性
    private Map<String, Double> normalizedAttrMap = new HashMap<>(); // 归一化属性
    
    // 常量定义
    public static final String ATTR_CAPACITY = "CAPACITY";
    public static final String ATTR_LISTPRICE = "LISTPRICE";
    public static final String ATTR_DELIVERYTIME = "DELIVERYTIME";
    public static final String ATTR_PROFIT = "PROFIT";
    public static final String ATTR_WEIGHT = "WEIGHT";
    public static final String ATTR_PRIORITY_SCORE = "PRIORITY_SCORE";
    
    // 设置原始属性
    public void setAttr(String attrName, double value) {
        attrMap.put(attrName, value);
    }
    
    // 获取原始属性
    public double getAttr(String attrName) {
        return attrMap.getOrDefault(attrName, 0.0);
    }
    
    // 设置归一化属性
    public void setNormalizedAttr(String attrName, double normalizedValue) {
        normalizedAttrMap.put(attrName, normalizedValue);
    }
    
    // 获取归一化属性
    public double getNormalizedAttr(String attrName) {
        return normalizedAttrMap.getOrDefault(attrName, 0.0);
    }
    
    // 检查是否包含某个属性
    public boolean hasAttr(String attrName) {
        return attrMap.containsKey(attrName);
    }
}


2、PriorityMultiSolver 类做下列修改
2.1  initParts 增加
  增加对listPrice和deliveryTime初始化

3、增加 归一化处理器
/**
 * 归一化处理器 - 负责将原始属性归一化到[0,100]范围
 * 独立于约束模型，只处理数据
 */
public class NormalizationProcessor {
    
    // 归一化配置
    @Data
    public static class NormalizationConfig {
        private String attrName;
        private NormalizationMethod method = NormalizationMethod.LINEAR;
        private double minValue;      // 可选：自定义最小值
        private double maxValue;      // 可选：自定义最大值
        private boolean isQuantityRelated = true; // 是否与数量相关
        private double power = 1.0;   // 幂律归一化的指数
        private int numBins = 10;     // 分箱数量（用于分位数归一化）
        
        public NormalizationConfig(String attrName) {
            this.attrName = attrName;
        }
    }
    
    // 归一化方法枚举
    public enum NormalizationMethod {
        LINEAR,      // 线性归一化
        LOG,         // 对数归一化
        LOG10,       // 以10为底的对数
        POWER,       // 幂律归一化
        QUANTILE,    // 分位数归一化
        RANK,        // 排名归一化
        PIECEWISE,   // 分段线性
        Z_SCORE      // Z-score归一化
    }
    
    // 归一化范围
    @Data
    public static class NormalizationRange {
        private double minValue;
        private double maxValue;
        private double meanValue;
        private double stdValue;
        private List<Double> sortedValues; // 排序后的值（用于分位数）
        
        public NormalizationRange(double min, double max) {
            this.minValue = min;
            this.maxValue = max;
        }
        
        public double getRange() {
            return maxValue - minValue;
        }
    }
    
    // 归一化结果
    @Data
    public static class NormalizationResult {
        private String attrName;
        private Map<String, Double> normalizedValues; // partCode -> 归一化值
        private NormalizationRange range;
        private NormalizationMethod method;
        
        public NormalizationResult(String attrName) {
            this.attrName = attrName;
            this.normalizedValues = new HashMap<>();
        }
        
        public void addNormalizedValue(String partCode, double value) {
            normalizedValues.put(partCode, value);
        }
        
        public double getNormalizedValue(String partCode) {
            return normalizedValues.getOrDefault(partCode, 50.0); // 默认50
        }
    }
    
    // 主归一化方法
    public NormalizationResult normalize(List<Part> parts, NormalizationConfig config) {
        // 收集原始值
        List<Double> rawValues = collectRawValues(parts, config.attrName);
        
        // 计算范围
        NormalizationRange range = calculateRange(rawValues, config);
        
        // 应用归一化方法
        return applyNormalization(parts, config, range);
    }
    
    // 收集原始值
    private List<Double> collectRawValues(List<Part> parts, String attrName) {
        List<Double> values = new ArrayList<>();
        for (Part part : parts) {
            if (part.hasAttr(attrName)) {
                values.add(part.getAttr(attrName));
            }
        }
        return values;
    }
    
    // 计算范围
    private NormalizationRange calculateRange(List<Double> values, NormalizationConfig config) {
        if (values.isEmpty()) {
            return new NormalizationRange(0, 1);
        }
        
        // 如果配置中指定了范围，使用配置的范围
        if (config.minValue != config.maxValue) {
            return new NormalizationRange(config.minValue, config.maxValue);
        }
        
        // 否则自动计算
        double min = Collections.min(values);
        double max = Collections.max(values);
        double sum = 0.0;
        
        for (Double val : values) {
            sum += val;
        }
        double mean = sum / values.size();
        
        // 计算标准差
        double variance = 0.0;
        for (Double val : values) {
            variance += Math.pow(val - mean, 2);
        }
        double std = Math.sqrt(variance / values.size());
        
        NormalizationRange range = new NormalizationRange(min, max);
        range.setMeanValue(mean);
        range.setStdValue(std);
        range.setSortedValues(new ArrayList<>(values));
        Collections.sort(range.getSortedValues());
        
        return range;
    }
    
    // 应用归一化
    private NormalizationResult applyNormalization(List<Part> parts, 
                                                  NormalizationConfig config, 
                                                  NormalizationRange range) {
        NormalizationResult result = new NormalizationResult(config.attrName);
        result.setRange(range);
        result.setMethod(config.method);
        
        for (Part part : parts) {
            if (!part.hasAttr(config.attrName)) {
                continue;
            }
            
            double rawValue = part.getAttr(config.attrName);
            double normalizedValue;
            
            switch (config.method) {
                case LINEAR:
                    normalizedValue = linearNormalize(rawValue, range);
                    break;
                case LOG:
                    normalizedValue = logNormalize(rawValue, range);
                    break;
                case LOG10:
                    normalizedValue = log10Normalize(rawValue, range);
                    break;
                case POWER:
                    normalizedValue = powerNormalize(rawValue, range, config.power);
                    break;
                case QUANTILE:
                    normalizedValue = quantileNormalize(rawValue, range, config.numBins);
                    break;
                case RANK:
                    normalizedValue = rankNormalize(rawValue, range);
                    break;
                case PIECEWISE:
                    normalizedValue = piecewiseNormalize(rawValue, range);
                    break;
                case Z_SCORE:
                    normalizedValue = zScoreNormalize(rawValue, range);
                    break;
                default:
                    normalizedValue = linearNormalize(rawValue, range);
            }
            
            // 确保在[0, 100]范围内
            normalizedValue = Math.max(0, Math.min(100, normalizedValue));
            
            // 存储结果
            result.addNormalizedValue(part.getCode(), normalizedValue);
            
            // 同时更新Part对象的归一化属性
            part.setNormalizedAttr(config.attrName, normalizedValue);
        }
        
        return result;
    }
    
    // 线性归一化
    private double linearNormalize(double value, NormalizationRange range) {
        if (range.getRange() == 0) return 50.0;
        return ((value - range.getMinValue()) / range.getRange()) * 100;
    }
    
    // 对数归一化
    private double logNormalize(double value, NormalizationRange range) {
        if (value <= 0) value = 0.1; // 避免log(0)
        double logValue = Math.log(value + 1);
        double logMin = Math.log(range.getMinValue() + 1);
        double logMax = Math.log(range.getMaxValue() + 1);
        
        if (logMax - logMin == 0) return 50.0;
        return ((logValue - logMin) / (logMax - logMin)) * 100;
    }
    
    // 以10为底的对数归一化
    private double log10Normalize(double value, NormalizationRange range) {
        if (value <= 0) value = 0.1;
        double log10Value = Math.log10(value + 1);
        double log10Min = Math.log10(range.getMinValue() + 1);
        double log10Max = Math.log10(range.getMaxValue() + 1);
        
        if (log10Max - log10Min == 0) return 50.0;
        return ((log10Value - log10Min) / (log10Max - log10Min)) * 100;
    }
    
    // 幂律归一化
    private double powerNormalize(double value, NormalizationRange range, double power) {
        double poweredValue = Math.pow(value, power);
        double poweredMin = Math.pow(range.getMinValue(), power);
        double poweredMax = Math.pow(range.getMaxValue(), power);
        
        if (poweredMax - poweredMin == 0) return 50.0;
        return ((poweredValue - poweredMin) / (poweredMax - poweredMin)) * 100;
    }
    
    // 分位数归一化
    private double quantileNormalize(double value, NormalizationRange range, int numBins) {
        List<Double> sortedValues = range.getSortedValues();
        if (sortedValues == null || sortedValues.isEmpty()) return 50.0;
        
        // 找到value在排序列表中的位置
        int index = Collections.binarySearch(sortedValues, value);
        if (index < 0) {
            index = -(index + 1); // 插入位置
        }
        
        // 计算百分位数
        double percentile = (double) index / sortedValues.size() * 100;
        return percentile;
    }
    
    // 排名归一化
    private double rankNormalize(double value, NormalizationRange range) {
        List<Double> sortedValues = range.getSortedValues();
        if (sortedValues == null || sortedValues.isEmpty()) return 50.0;
        
        // 计算排名（从1开始）
        int rank = 1;
        for (Double val : sortedValues) {
            if (val < value) rank++;
        }
        
        // 归一化到[0, 100]
        double normalizedRank = ((double) rank / sortedValues.size()) * 100;
        return normalizedRank;
    }
    
    // 分段线性归一化（简化版）
    private double piecewiseNormalize(double value, NormalizationRange range) {
        // 简单实现：三等分
        double segment = range.getRange() / 3;
        
        if (value <= range.getMinValue() + segment) {
            return 33.3 * (value - range.getMinValue()) / segment;
        } else if (value <= range.getMinValue() + 2 * segment) {
            return 33.3 + 33.3 * (value - range.getMinValue() - segment) / segment;
        } else {
            return 66.6 + 33.3 * (value - range.getMinValue() - 2 * segment) / segment;
        }
    }
    
    // Z-score归一化
    private double zScoreNormalize(double value, NormalizationRange range) {
        double zScore = (value - range.getMeanValue()) / range.getStdValue();
        // 将Z-score映射到[0, 100]，假设大部分数据在[-3, 3]之间
        double normalized = 50 + (zScore * 50 / 3);
        return Math.max(0, Math.min(100, normalized));
    }
    
    // 批量归一化多个属性
    public Map<String, NormalizationResult> normalizeBatch(List<Part> parts, 
                                                          List<NormalizationConfig> configs) {
        Map<String, NormalizationResult> results = new HashMap<>();
        
        for (NormalizationConfig config : configs) {
            NormalizationResult result = normalize(parts, config);
            results.put(config.getAttrName(), result);
        }
        
        return results;
    }
}

5、增加目标构建器（表达式构建独立）
/**
 * 目标构建器 - 负责构建目标函数的线性表达式
 * 使用归一化后的数据，但不包含归一化逻辑
 */
public class ObjectiveExpressionBuilder {
    private CpModelTracker model;
    private List<PartVar> partVars;
    private Map<String, PartVar> partVarMap; // code -> PartVar
    
    public ObjectiveExpressionBuilder(CpModelTracker model, List<PartVar> partVars) {
        this.model = model;
        this.partVars = partVars;
        this.partVarMap = new HashMap<>();
        
        for (PartVar pv : partVars) {
            partVarMap.put(pv.getCode(), pv);
        }
    }
    
    // 目标配置
    @Data
    public static class ObjectiveConfig {
        private String name;
        private ObjectiveType type;
        private double weight;            // 归一化后的权重
        private String attrName;          // 属性名称
        private boolean quantityRelated = true; // 是否与数量相关
        private AggregateType aggregateType = AggregateType.SUM; // 聚合类型
        
        public ObjectiveConfig(String name, ObjectiveType type, double weight, String attrName) {
            this.name = name;
            this.type = type;
            this.weight = weight;
            this.attrName = attrName;
        }
    }
    
    // 目标类型
    public enum ObjectiveType {
        MAXIMIZE,    // 最大化
        MINIMIZE     // 最小化
    }
    
    // 聚合类型
    public enum AggregateType {
        SUM,         // 求和（数量相关）
        MAX,         // 最大值（独立目标）
        MIN,         // 最小值（独立目标）
        AVG          // 平均值
    }
    
    // 构建单个目标表达式
    public TrackedTrackedLinearExpr buildExpression(ObjectiveConfig config) {
        switch (config.aggregateType) {
            case SUM:
                return buildSumExpression(config);
            case MAX:
                return buildMaxExpression(config);
            case MIN:
                return buildMinExpression(config);
            case AVG:
                return buildAvgExpression(config);
            default:
                throw new IllegalArgumentException("不支持的聚合类型: " + config.aggregateType);
        }
    }
    
    // 构建求和表达式（数量相关）
    private TrackedTrackedLinearExpr buildSumExpression(ObjectiveConfig config) {
        TrackedTrackedLinearExpr sumExpr =  
        
        for (PartVar pv : partVars) {
            Part part = pv.getPart();
            
            // 获取归一化属性值
            Double normalizedValue = part.getNormalizedAttr(config.getAttrName());
            if (normalizedValue == null) {
                // 如果没有归一化值，尝试使用原始值（应该不会发生）
                normalizedValue = part.getAttr(config.getAttrName());
            }
            
            // 创建表达式：quantity * normalizedValue
            sumExpr.addTerm(pv.getQuantity(), normalizedValue.longValue());
        }
        
        return sumExpr.build();
    }
    
    // 构建最大值表达式（独立目标）
    private TrackedTrackedLinearExpr buildMaxExpression(ObjectiveConfig config) {
        // 创建最大值变量
        IntVar maxVar = model.newIntVar(0, 100, "max_" + config.getName());
        
        // 创建每个部件的值变量
        List<IntVar> partValueVars = new ArrayList<>();
        
        for (PartVar pv : partVars) {
            Part part = pv.getPart();
            Double normalizedValue = part.getNormalizedAttr(config.getAttrName());
            int value = (int) Math.round(normalizedValue);
            
            // 创建部件值变量
            IntVar partValue = model.newIntVar(0, value, 
                "partval_" + pv.getCode() + "_" + config.getAttrName());
            
            // 约束：如果部件被选中，partValue = value；否则为0
            IntVar isSelected = pv.getIsSelected();
            
            // 当isSelected=1时，partValue=value
            model.addEquality(partValue, value).onlyEnforceIf(isSelected);
            // 当isSelected=0时，partValue=0
            model.addEquality(partValue, 0).onlyEnforceIf(isSelected.not());
            
            partValueVars.add(partValue);
        }
        
        // 设置maxVar为所有partValue的最大值
        model.addMaxEquality(maxVar, partValueVars.toArray(new IntVar[0]));
        
        return TrackedTrackedLinearExpr.add(maxVar).build();
    }
    
    // 构建最小值表达式（独立目标）
    private TrackedTrackedLinearExpr buildMinExpression(ObjectiveConfig config) {
        // 创建最小值变量
        IntVar minVar = model.newIntVar(0, 100, "min_" + config.getName());
        
        // 创建每个部件的值变量
        List<IntVar> partValueVars = new ArrayList<>();
        
        for (PartVar pv : partVars) {
            Part part = pv.getPart();
            Double normalizedValue = part.getNormalizedAttr(config.getAttrName());
            int value = (int) Math.round(normalizedValue);
            
            // 创建部件值变量
            // 注意：对于最小值，当部件没选中时，我们希望它不影响最小值
            // 所以设置一个很大的值（如100）当部件没选中时
            IntVar partValue = model.newIntVar(0, 100, 
                "partval_" + pv.getCode() + "_" + config.getAttrName());
            
            IntVar isSelected = pv.getIsSelected();
            
            // 当isSelected=1时，partValue=value
            model.addEquality(partValue, value).onlyEnforceIf(isSelected);
            // 当isSelected=0时，partValue=100（不影响最小值）
            model.addEquality(partValue, 100).onlyEnforceIf(isSelected.not());
            
            partValueVars.add(partValue);
        }
        
        // 设置minVar为所有partValue的最小值
        model.addMinEquality(minVar, partValueVars.toArray(new IntVar[0]));
        
        return TrackedTrackedLinearExpr.add(minVar).build();
    }
    
    // 构建平均值表达式
    private TrackedLinearExpr buildAvgExpression(ObjectiveConfig config) {
        // 计算总和
        TrackedLinearExpr sumExpr = buildSumExpression(config);
        
        // 计算选中部件数量
        IntVar selectedCount = buildSelectedCount();
        
        // 创建平均值变量
        IntVar avgVar = model.newIntVar(0, 100, "avg_" + config.getName());
        
        // 约束：avgVar * selectedCount = sumExpr
        // 由于CP-SAT不支持除法，我们使用近似或线性化
        // 简化：直接使用总和，在目标函数中调整权重
        
        return sumExpr; // 简化返回总和
    }
    
    // 构建选中部件数量表达式
    private IntVar buildSelectedCount() {
        IntVar countVar = model.newIntVar(0, partVars.size(), "selected_count");
        
        TrackedTrackedLinearExpr countExpr = TrackedTrackedLinearExpr;
        for (PartVar pv : partVars) {
            // 使用isSelected（0/1）求和
            countExpr.add(pv.getIsSelected());
        }
        
        model.addEquality(countVar, countExpr.build());
        return countVar;
    }
    
    // 构建多个目标表达式的综合目标
    public TrackedLinearExpr buildCompositeObjective(List<ObjectiveConfig> configs) {
        Map<String, TrackedLinearExpr> expressions = new HashMap<>();
        
        // 构建所有表达式
        for (ObjectiveConfig config : configs) {
            TrackedLinearExpr expr = buildExpression(config);
            expressions.put(config.getName(), expr);
        }
        
        // 构建综合目标
        return combineExpressions(configs, expressions);
    }
    
    // 合并多个表达式为目标函数
    private TrackedLinearExpr combineExpressions(List<ObjectiveConfig> configs, 
                                         Map<String, TrackedLinearExpr> expressions) {
        TrackedTrackedLinearExpr composite = TrackedTrackedLinearExpr;
        
        for (ObjectiveConfig config : configs) {
            TrackedLinearExpr expr = expressions.get(config.getName());
            
            if (config.getType() == ObjectiveType.MAXIMIZE) {
                // 最大化：负权重（因为CP-SAT是最小化）
                composite.addTerm(expr, -(long)(config.getWeight() * 1000));
            } else {
                // 最小化：正权重
                composite.addTerm(expr, (long)(config.getWeight() * 1000));
            }
        }
        
        return composite.build();
    }
    
    // 创建常用的目标配置
    public static List<ObjectiveConfig> createCommonObjectives() {
        List<ObjectiveConfig> configs = new ArrayList<>();
        
        // 数量相关目标（求和）
        configs.add(new ObjectiveConfig("totalCost", ObjectiveType.MINIMIZE, 1.0, Part.ATTR_LISTPRICE));
        configs.add(new ObjectiveConfig("totalWeight", ObjectiveType.MAXIMIZE, 0.8, Part.ATTR_WEIGHT));
        configs.add(new ObjectiveConfig("totalProfit", ObjectiveType.MAXIMIZE, 1.2, Part.ATTR_PROFIT));
        configs.add(new ObjectiveConfig("totalCapacity", ObjectiveType.MAXIMIZE, 1.5, Part.ATTR_CAPACITY));
        
        // 独立目标（最大值/最小值）
        ObjectiveConfig deliveryConfig = new ObjectiveConfig("maxDelivery", 
            ObjectiveType.MINIMIZE, 0.7, Part.ATTR_DELIVERYTIME);
        deliveryConfig.setQuantityRelated(false);
        deliveryConfig.setAggregateType(AggregateType.MAX);
        configs.add(deliveryConfig);
        
        ObjectiveConfig priorityConfig = new ObjectiveConfig("minPriority", 
            ObjectiveType.MAXIMIZE, 0.5, Part.ATTR_PRIORITY_SCORE);
        priorityConfig.setQuantityRelated(false);
        priorityConfig.setAggregateType(AggregateType.MIN);
        configs.add(priorityConfig);
        
        return configs;
    }
}

6、修改PriorityMultiSolver，使用上述类

7、调通PriorityMultiSolverTest一个用例



normalized_cost = (cost - min_cost) / (max_cost - min_cost) * 100
但是注意，我们希望成本越小越好，所以我们可以用反向归一化：
cost_score = 100 - normalized_cost

归一化：
对于cost，我们可以预先计算一个最大成本max_cost（所有部件都选最贵的）和最小成本min_cost（所有部件都选最便宜的），然后：
normalized_cost = (cost - min_cost) / (max_cost - min_cost) * 100
但是注意，我们希望成本越小越好，所以我们可以用反向归一化：
cost_score = 100 - normalized_cost

对于weight，类似地计算最大重量max_weight和最小重量min_weight，然后：
normalized_weight = (weight - min_weight) / (max_weight - min_weight) * 100
我们希望重量越大越好，所以weight_score = normalized_weight

对于delivery，我们计算最大货期max_delivery和最小货期min_delivery，注意delivery是最大货期（即所有选中部件中货期的最大值），同样反向：
normalized_delivery = (delivery - min_delivery) / (max_delivery - min_delivery) * 100
delivery_score = 100 - normalized_delivery


总目标函数：
total_score = w1 * cost_score + w2 * weight_score + w3 * delivery_score
我们希望最大化total_score，即最小化 -total_score。

2026-1-25：
TODO: 为什么没有min/max等目标函数，也有OPTIMAL，为什么？

idea:
现象：
strReq: Capacity >=5 
solverStatus:OPTIMAL
solutions size:3
Solu1: parts=[sd1 Q:0, sd2 Q:1, sd3 Q:0, md1 Q:0, md2 Q:0, md3 Q:0], OV=400.0
Solu2: parts=[sd1 Q:0, sd2 Q:0, sd3 Q:0, md1 Q:0, md2 Q:1, md3 Q:1], OV=1005.0
Solu3: parts=[sd1 Q:0, sd2 Q:0, sd3 Q:0, md1 Q:0, md2 Q:0, md3 Q:2], OV=1506.0

 26. - 100 * (3 * sd1_Q + 6 * sd2_Q + 9 * sd3_Q) + 1 * (1 * md1_Q + 2 * md2_Q + 3 * md3_Q) + 500 * (1 * (3 * sd1_Q + 6 * sd2_Q + 9 * sd3_Q + 1 * md1_Q + 2 * md2_Q + 3 * md3_Q) - 5) + 500 * (1 * sd1_Q + 1 * sd2_Q + 1 * sd3_Q + 1 * md1_Q + 1 * md2_Q + 1 * md3_Q)   (minimize) L:ObjectiveFun


 12. 1500 * (20 * sd1_Q + 0 * md1_Q) - 2000 * (25 * sd1_Q + 0 * md1_Q) - 1200 * (25 * sd1_Q + 0 * md1_Q) + 700 * (33 * sd1_Q + 0 * md1_Q) + 500 * (1 * (3 * sd1_Q + 1 * md1_Q) - 6) + 500 * (1 * sd1_Q + 1 * md1_Q) <= 0 (addLessOrEqual) L:composite_objective


TODO: 带数量的标准化估计还是有问题？***
TODO： 2*2 = 5    3*15 = 45？--总量，感觉不行***

            // objectValue设置太小，很多可用解没有挖掘出来, 系数需要往上调
            // objectValue设置太大，太多的解，导致的最优解都没有处理(result.hasSearchMax=false)，系数需要往下调

框计算：
https://www.toutiao.com/video/7598370786490974730/?from_scene=all&log_from=8956a2a665d3c_1769382567799



idea: 
 最优解可能也有多个？
sd1.qty=2  问题：存在求到最优解了，就终止掉了 --min

CpSolverStatus:OPTIMAL 
 
2026-1-24：

参数上增加一个
1、
Parameter，是否
bool  inputType;///看一下现在的crs？ ---？ 
reletivePro:  capacity.sum  -->
如果是输入类型  
  
TotalCapacity 

有两种配置方式：
1、直接告诉数量，直接算数量
  做相关校验即可
2、



多种配置路径？


TODO:多个被选中
   // 最多只能有一种固态硬盘被选中


	

TODO：开放一个模块来做这个事情？ --- 一个模型不能完全表达这个意思


如果是带CPU的案例，组合类型，先求关键？ 






req1: 

对产品级的规则，使用扣除 
对过滤规则，还是使用的过滤规则？ 

req1 
  分类1：
  
  filterPartCodes //过滤后的partCods

req2
  分类2：
  filterPartCodes：


几种情况：
案例：
req1:  没有满足要求的？
 req1：  要求3000速率的硬盘是2块，没有满足要求，直接无解？ 


TODO: 多个优选规则的是否有冲突？ ---？ --怎么办？ **8
  % 百分不好用

TODO：这两边的代码合并在一起，使用claude看一下，是否可以？   把本体结构构建起来？ ***
--三个代码？ ---
--单层？

---先求关键part

TODO: 在把yuzhi的例子搞进来 ?**


场景1：
分类之间是包含关系   req2=sd,sd3  --> req1->    md1,sd1,sd2,sd3  --大很少？ 有一个边没有 
    // req1 要求9900速率的硬盘容量>=8T(其它容量不限制？）     req1=drive, md4,   md4.Q * 4 >=8   全量有
	// req2 要求5400速率的固态硬盘数量是2个  req2    req2=sd sd1,sd2     其它不限制？  sd1.Q + sd2.Q = 2  
每个part都有变量的  和在一起？  ==》  md4,sd1,sd2  

场景1： 
    
TODO:考虑属性的多实例？ 
  req2： 要求5400速率的固态硬盘的总容量大于=8%？
  为什么要使用Part clone？ *** ，  如果没有多inst ,就不需要赋值？
   



req1 和 req2两种理解？ ---一种是的 and， 一种或



分类之间是包含关系    req1=drive, md1,sd1,sd2  req2=sd,sd3  --> req1->    md1,sd1,sd2,sd3  --按分类相加
分类之间的并列关系    req1=md, md1   req2=sd,sd3  --> req1->  md1，sd3
分类之间
维度，同一个分类？ 

单个，但是没有包括另外半边 
req1 = 9900速率的机械硬盘容量>=8T  md4:4T   md4.Q*4 >=8  最少配吧？  md1~md3也是可以配置的？  汇总的数量**  rule1的的规则没有名字，使用过滤法是不行的？
req2 = 总容量大量16T    sd1.Q*1 + sd2. Q  **       最优解？   卡一下？ 



---另外版本没有算法

TODO：“固态硬盘必须配置同一种” ---单选PartCategory ***
  






---req=“

TODO：不同分类的也不同？ ---**  同类产品，不同类产品的？  过滤的规则怎么写？ 
按分类相加
---硬盘 
----机械硬盘  
----固态硬盘

---CPU 
----cpu11
----cpu11

TODO：搞一个简单的例子


多条件组合的问题?  *** 

--分类1：

--




TODO：表示没有满足条件，--如果是Nulll，不满足，则要返回？ ---？ 


PartCategory  

---分类1 


先过滤再计算 ---

  计算---




TODO：
1、是不是把小的sqlite引入进来  doSelect
1、实现多个条件排序？
2、多个目标值求解？ 有一个的 --排序 
一次只能有个目标函数？ 
---最大，最小？

---先标准化？    --放到同一个全局
 
---写一个例子处理？ --文档，处理？ 

标准化，在加权总
 
100   
Relax  ---10

Product -- 1
Level5  --1
Level4  --0.9
Levle5  --0.8
Levle4 ---0.7
Level5----0.
最多同时支持5个;


TODO：where 条件复杂化？ 

TODO: 计算一个最大值乎， >=, 需要normal 把他均匀化，方便能搜索到？ ---**   = PriorityType.SELECT)  如何数量 --- 如果 

TODO： 价格优先 ，可能物理约束不一定度满足？  --***
 ---为什么不能算后，再按 多维去的排序？ ---因为有 优先条件，可能价格的因素考到？ 
----优先使用？ 


TODO：多解？ ---**
---多个用户的过滤条件？ 
---
---CPU和电脑组合的问题？ ---**

-----未来组合是不是两种？ 
-----子模块，和子模块自己的约束？ ---> 先计算一个范围和结果？ 


---方案1：
 ---分层？  --产品级？ ---先把维度减少?  做参数直接相关过滤？ 

 ---方案，放在一起  ---
  ---关键分类直接的计算---》  放在电脑层级？ ————




     part1A，
---数量计算，这个很麻烦？ ---？

各种案例？ ---

  分类和分类直接有 属性直接的约束  变成 部件集合1  部件集合2 的关系？ 
   如果 CPU配置了 dell，则硬盘 智能是 5400转的、
  分类数量和分类数量直接的直接的约束？   硬盘的数量= CPU的数量
  分类数据和 另外一个分类属性的关系   电脑的数量  = CPU的接口数  *2  
  混合： 
维度1： 和类型相关
维度2： 表达是的复杂度，简单表达式还是复杂的 while 等？ 


  单选的分类
  多选分类 --- select的关系？ ————-


--
---先过滤（筛选法，扣除法），在求解

---分层计算？ **--参数和参数直接？ ---

--- --TODO：
---性能模型？  ---5个分类，每个分类有300个铺分类，每个分类 10个条约束，产品  ---  reqCon 100% req  30%过滤量， 性能分别是多少？ --** 单最优解， 5个推荐解 10个推荐解多长时间？ ***
---没和都已经先提前计算好？  
----参数和参数之间？ ---性能--对这块的算法？ ***


TODO：提取标准化和 最大化标准化有什么区别？ 
提取标准化：
part1...P100
  --ListPrice  --1,10,100,50000  --< (01,


TODO:
  PartCategory也是一种Module   --Module? PC

TODO: 规则需要单独生成

TODO: 提供查询能力等？

TODO: 优化 
---> @PartAnno(field=solidDrive, attrs={"BrandWidth:BW_16GB","Speed:MaxCapacity_16T"}) @VAttrAnno(field=solidDrive, dynAttrs=v"1:BW_16GB","2:MaxCapacity_16T"})

TODO:   --这个有问题？
  // 选项编码ID
    private int codeId;  ---》codeValue是不是更合适一点


TODO: attr
    "dynAttrSchemas" : [ "java.util.ArrayList", [ ] ],
    "dynAttrSchema" : null


先过滤，在构建约束条件？ ***

多个约束请求？ 都变成and？ 
  A or B  ==> A and ~B  吗？ TODO 
 
//表示2，是静态的都放到这里？ 
class PartConstraintReq {
	partCatagory = "drive"
	attrCode="Capacity" //单个硬盘
	attrComparator="=="
	attrValue="2" 
	attrWhereCondition=""
}

TODO: 非inst属性和属性不能混合在一起？

TODO：把CpSolver也包装起来

TODO: 求出这个解后，需要把业务模型？ 

TODO：怎么让生成的代码 符合编程规范？ ---不需要再调整？**

1、老的不分？
2、新的？*