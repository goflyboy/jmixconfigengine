# 参数反推求解测试框架设计文档

## 1. 概述

### 1.1 设计目标
设计一个自动化测试框架，用于验证约束算法类的参数反推求解功能。该框架能够根据约束算法类的注解自动生成测试数据，并提供完整的测试执行和结果验证能力。

### 1.2 核心功能
- 根据约束算法类的注解，自动生成Module数据
- 自动初始化执行引擎和基础数据
- 提供流水结果比较框架，简化测试代码编写

### 1.3 使用流程
1. 手工创建约束算法类，如`HelloConstraint`
2. 手工创建对应的测试类，继承测试框架：如`HelloConstraintTest`
3. 手工补充测试用例（使用`ModuleScenarioTestBase`的assert方法等）

## 2. 架构设计

### 2.1 整体架构
```
测试框架基类 (ModuleScenarioTestBase)
    ↓
注解处理器 (ModuleGenneratorByAnno)
    ↓
约束执行引擎 (ModuleConstraintExecutorImpl)
    ↓
结果验证器 (ProgammableInstAssert)
```

### 2.2 核心组件
- **测试框架基类**: 提供测试基础设施和通用方法
- **注解处理器**: 解析注解并生成Module数据
- **结果验证器**: 提供链式断言API进行结果验证

## 3. 详细设计

### 3.1 测试框架基类设计

#### 3.1.1 类结构
```java
public abstract class ModuleScenarioTestBase {
    // 核心属性
    protected Class<? extends ConstraintAlg> constraintAlgClazz;
    protected String tempResourcePath = "";
    protected Module module;
    protected ModuleConstraintExecutor.ConstraintConfig cfg;
    protected ModuleConstraintExecutorImpl exec;
    protected List<ModuleConstraintExecutor.ModuleInst> solutions;
    
    // 核心方法
    protected void initModule();
    protected void initExecutor();
    protected List<ModuleConstraintExecutor.ModuleInst> inferParas(String partCode, Integer qty, String... paraCodeValuePairs);
    protected ProgammableInstAssert assertSolution(int index);
    protected void printSolutions();
}
```

#### 3.1.2 核心方法设计

##### initModule() - 初始化Module
```java
protected void initModule() {
    // 伪代码
    if (module == null) {
        module = ModuleGenneratorByAnno.generateModule(constraintAlgClazz, tempResourcePath);
    }
}
```

##### initExecutor() - 初始化执行引擎
```java
protected void initExecutor() {
    // 伪代码
    if (exec == null) {
        exec = new ModuleConstraintExecutorImpl();
        cfg = new ConstraintConfig();
        // 配置执行引擎
    }
}
```

##### inferParas() - 参数反推求解
```java
protected List<ModuleConstraintExecutor.ModuleInst> inferParas(String partCode, Integer qty, String... paraCodeValuePairs) {
    // 伪代码
    InferParasReq req = buildInferParasReq(partCode, qty, paraCodeValuePairs);
    Result result = exec.inferParas(req);
    solutions = result.getModuleInsts();
    return solutions;
}
```

##### assertSolution() - 结果断言
```java
protected ProgammableInstAssert assertSolution(int index) {
    // 伪代码
    ModuleInst solution = solutions.get(index);
    return new ProgammableInstAssert(solution);
}
```

##### printSolutions() - 打印解决方案
```java
protected void printSolutions() {
    // 伪代码
    for (int i = 0; i < solutions.size(); i++) {
        log.info("解决方案 {}: {}", i, toJson(solutions.get(i)));
    }
}
```

### 3.2 注解处理器设计

#### 3.2.1 核心注解定义

##### ModuleAnno注解
```java
@Target(ElementType.TYPE)
@Retention(RetentionPolicy.RUNTIME)
public @interface ModuleAnno {
    Long id();
    String code() default "";
    String packageName() default "";
    String version() default "1.0.0";
    String description() default "";
    Integer sortNo() default 0;
    String extSchema() default "";
    String[] extAttrs() default {};
}
```

##### ParaAnno注解
```java
@Target(ElementType.FIELD)
@Retention(RetentionPolicy.RUNTIME)
public @interface ParaAnno {
    String code() default "";
    String fatherCode() default "";
    String defaultValue() default "";
    String description() default "";
    Integer sortNo() default 0;
    ParaType type() default ParaType.INTEGER;
    String extSchema() default "";
    String[] extAttrs() default {};
    String[] options() default {};
}
```

##### PartAnno注解
```java
@Target(ElementType.FIELD)
@Retention(RetentionPolicy.RUNTIME)
public @interface PartAnno {
    String code() default "";
    String fatherCode() default "";
    String description() default "";
    Integer sortNo() default 0;
    Integer maxQuantity() default 1;
    String extSchema() default "";
    String[] extAttrs() default {};
}
```

#### 3.2.2 注解处理器实现

##### generateModule() - 生成Module
```java
public static Module generateModule(Class<? extends ConstraintAlg> moduleAlgClazz, String resourcePath) {
    // 伪代码
    Module module = new Module();
    
    // 1. 处理类级别注解
    ModuleAnno moduleAnno = moduleAlgClazz.getAnnotation(ModuleAnno.class);
    if (moduleAnno != null) {
        setModuleProperties(module, moduleAnno, moduleAlgClazz);
    }
    
    // 2. 处理字段注解
    List<Para> paras = createParasFromFields(moduleAlgClazz);
    List<Part> parts = createPartsFromFields(moduleAlgClazz);
    
    module.setParas(paras);
    module.setParts(parts);
    
    // 3. 保存到文件
    saveModuleToFile(module, resourcePath);
    
    return module;
}
```

##### createParasFromFields() - 创建参数
```java
private static List<Para> createParasFromFields(Class<? extends ConstraintAlg> clazz) {
    // 伪代码
    List<Para> paras = new ArrayList<>();
    
    for (Field field : clazz.getDeclaredFields()) {
        if (isParaVarField(field)) {
            ParaAnno paraAnno = field.getAnnotation(ParaAnno.class);
            if (paraAnno != null) {
                Para para = createParaFromAnnotation(field, paraAnno);
                paras.add(para);
            }
        }
    }
    
    return paras;
}
```

##### createPartsFromFields() - 创建部件
```java
private static List<Part> createPartsFromFields(Class<? extends ConstraintAlg> clazz) {
    // 伪代码
    List<Part> parts = new ArrayList<>();
    
    for (Field field : clazz.getDeclaredFields()) {
        if (isPartVarField(field)) {
            PartAnno partAnno = field.getAnnotation(PartAnno.class);
            if (partAnno != null) {
                Part part = createPartFromAnnotation(field, partAnno);
                parts.add(part);
            }
        }
    }
    
    return parts;
}
```

### 3.3 结果验证器设计

#### 3.3.1 ProgammableInstAssert类
```java
public class ProgammableInstAssert {
    private ModuleInst moduleInst;
    
    // 构造函数
    public ProgammableInstAssert(ModuleInst moduleInst);
    
    // 链式断言方法
    public ProgammableInstAssert hasParaValue(String paraCode, String expectedValue);
    public ProgammableInstAssert hasPartQuantity(String partCode, Integer expectedQty);
    public ProgammableInstAssert hasParaHidden(String paraCode, Boolean expectedHidden);
    public ProgammableInstAssert hasPartHidden(String partCode, Boolean expectedHidden);
    public ProgammableInstAssert hasSolutionCount(int expectedCount);
    public ProgammableInstAssert hasNoSolution();
    public ProgammableInstAssert hasSolution();
}
```

#### 3.3.2 断言方法实现
```java
public ProgammableInstAssert hasParaValue(String paraCode, String expectedValue) {
    // 伪代码
    ParaInst paraInst = findParaInst(moduleInst, paraCode);
    assertNotNull(paraInst, "参数不存在: " + paraCode);
    assertEquals(expectedValue, paraInst.getValue(), "参数值不匹配");
    return this;
}

public ProgammableInstAssert hasPartQuantity(String partCode, Integer expectedQty) {
    // 伪代码
    PartInst partInst = findPartInst(moduleInst, partCode);
    assertNotNull(partInst, "部件不存在: " + partCode);
    assertEquals(expectedQty, partInst.getQuantity(), "部件数量不匹配");
    return this;
}
```

## 4. 使用示例

### 4.1 约束算法类示例
```java
@ModuleAnno(id = 1L, code = "Hello", description = "Hello约束算法")
public class HelloConstraint extends ConstraintAlgImpl {
    
    @ParaAnno(code = "Color", type = ParaType.ENUM, options = {"Red", "Blue", "Green"})
    private ParaVar colorVar;
    
    @ParaAnno(code = "Size", type = ParaType.ENUM, options = {"S", "M", "L"})
    private ParaVar sizeVar;
    
    @PartAnno(code = "TShirt", maxQuantity = 1)
    private PartVar tshirtVar;
    
    // 约束规则方法
    public void colorSizeRule() {
        // 约束逻辑
    }
}
```

### 4.2 测试类示例
```java
public class HelloConstraintTest extends ModuleScenarioTestBase {
    
    @Before
    public void setUp() {
        constraintAlgClazz = HelloConstraint.class;
        tempResourcePath = "src/test/resources/temp";
        initModule();
        initExecutor();
    }
    
    @Test
    public void testColorSizeConstraint() {
        // 执行参数反推
        List<ModuleInst> solutions = inferParas("TShirt", 1, "Color", "Red", "Size", "M");
        
        // 验证结果
        assertSolution(0)
            .hasParaValue("Color", "Red")
            .hasParaValue("Size", "M")
            .hasPartQuantity("TShirt", 1);
    }
    
    @Test
    public void testNoSolution() {
        // 测试无解情况
        List<ModuleInst> solutions = inferParas("TShirt", 1, "Color", "Invalid");
        
        // 验证无解
        assertSolution(0).hasNoSolution();
    }
}
```

## 5. 扩展功能

### 5.1 自定义断言方法
```java
public class CustomProgammableInstAssert extends ProgammableInstAssert {
    
    public CustomProgammableInstAssert hasValidColorSize() {
        // 伪代码
        String color = getParaValue("Color");
        String size = getParaValue("Size");
        assertTrue(isValidColorSizeCombination(color, size), "颜色尺寸组合无效");
        return this;
    }
}
```

### 5.2 批量测试支持
```java
@Test
public void testAllColorSizeCombinations() {
    String[] colors = {"Red", "Blue", "Green"};
    String[] sizes = {"S", "M", "L"};
    
    for (String color : colors) {
        for (String size : sizes) {
            List<ModuleInst> solutions = inferParas("TShirt", 1, "Color", color, "Size", size);
            assertSolution(0)
                .hasParaValue("Color", color)
                .hasParaValue("Size", size);
        }
    }
}
```

## 6. 最佳实践

### 6.1 测试用例设计原则
- 每个测试用例只验证一个约束规则
- 使用有意义的测试数据
- 包含边界条件和异常情况测试
- 保持测试用例的独立性

### 6.2 性能优化建议
- 使用@BeforeClass进行一次性初始化
- 避免在测试中创建大量临时数据
- 合理使用断言，避免过度验证

### 6.3 维护建议
- 定期更新测试数据
- 保持注解和测试用例的同步
- 及时清理过时的测试用例

## 7. 总结

该测试框架通过注解驱动的方式，大大简化了约束算法类的测试编写工作。开发者只需要：

1. 在约束算法类上添加适当的注解
2. 继承测试框架基类
3. 编写简单的测试用例

框架会自动处理Module生成、执行引擎初始化、结果验证等复杂工作，让开发者专注于业务逻辑的测试。